# 小红书一键收录 v3 — 多租户团队版 PRD

> **文档版本**：v3.1（含 2026-05-22 二轮澄清后的修订）
> **编制时间**：2026-05-22 凌晨
> **操盘人**：王小熊
> **当前状态**：所有 blocking 已澄清，进入 Ready 状态，等小熊批 PRD 后执行

---

## 🔥 v3.1 修订（二轮澄清后的关键变更）

| 决策项 | v3.0 原方案 | **v3.1 最终方案** |
|---|---|---|
| 10 用户身份 | 假设同租户 | ✅ **确认同租户**（公司同事，免费飞书，同公司组织） |
| 推进节奏 | 分 3 阶段（你→柯希安→10 人） | ⚠️ **改为一次做完，10 人一起上线** |
| 飞书表加列 | 加 O 列「所有图」 | ✅ **不加列**，只保留 E 列封面 |
| 截图功能 | 自动存所有图 | ✅ **撤销**，只存封面 |
| 总工时 | 28-29h（分 3 次） | ✅ **~20h，分 2 次会话** |
| 中间验证 | 每阶段都有验收 | ⚠️ **没有中间验证**，一次到位 |

**用户已知风险并接受**：若架构有坑，10 人全部受影响。Stop Condition：发现根本性问题立刻停下问小熊。

---

## 0. 项目定位重大变化（必读）

**原定位**（v1/v2）：给小熊一个人用的电脑工具
**新定位**（v3）：**给团队 10 人用的多租户工具**

这不是"加功能"，是**架构重做**。复杂度翻 3-5 倍。

---

## 1. 关键决策日志（你已拍板，固化到文档）

| 决策项 | 选择 | 含义 |
|---|---|---|
| **B1 多租户模型** | 共用后端 + 每人独立飞书表 | 一个服务器服务 10 人；每人在扩展里填自己的飞书表链接 |
| **B2 数据结构** | 起号 sheet + 爆款 sheet + 其他每标签一个 sheet | 一个用户表里有 N 个 sheet |
| **C1 标签创建** | 扩展里加标签 → 自动建 sheet | 用户零飞书操作，后端代办 |
| **C2 截图功能** | 自动存所有图 → 飞书表加「图片列」 | 不让用户选图，一次性全存 |
| **C3 关键词自动分类** | 不做 | 简化范围，用户手选 |
| **C4 MVP 节奏** | 小熊 → 柯希安 → 10 人 | 三阶段验证后扩展 |

---

## 2. Assumptions（暂时假设，请你看一眼有没有错）

- A1：bot 应用「刘亦菲」（cli_a90ee8a787225bd8）会被加到 10 个团队成员的飞书表里
- A2：团队成员都在你公司同一飞书租户内（共 100 人那个组织）
- A3：14.22.112.147 服务器资源（4 核/7.5G）够 10 人低频并发用
- A4：每个用户的飞书表结构一致（同样的 14 列 + 同样的固定 sheet 名）
- A5：用户加入时由你或柯希安统一帮他把 bot 加为协作者（不做自助流程）

如果 A1-A5 任一不成立，告诉我，会回到 Clarity Gate。

---

## 3. 非目标（不做的事）

明确列出 **不做**，防止范围漂移：

- ❌ 不做支付、不做 SaaS 公开注册
- ❌ 不做 OAuth 用户授权流程（用 bot + 协作者就够）
- ❌ 不做关键词自动分类（C3 已否决）
- ❌ 不做 AI 文案拆解（这是更后阶段）
- ❌ 不做飞书群机器人入口
- ❌ 不做小红书账号登录后才能爬取的私密内容
- ❌ 不动现有服务器其他业务（bookboy/gold-rush/xiaoxiong-workbench）

---

## 4. 分阶段交付计划

### Phase 2.0 — 小熊个人版完整功能（**先做这个**）

🎯 目标：先让你自己用得爽，所有新功能你都能验证

| # | 功能 | 工时 |
|---|---|---|
| 1 | 「分类 sheet 管理」：扩展加标签 → 自动建飞书 sheet | 3h |
| 2 | popup 改单选下拉（起号 / 爆款 / 自定义标签） | 1h |
| 3 | 每条笔记**自动存所有图**到新增「图片列」O | 1.5h |
| 4 | 历史记录 + 仪表盘（顶部数字 + 底部最近 5 条） | 2h |
| 5 | 失败列表 + 一键重试 | 1h |
| 6 | 重复策略选项（跳过/覆盖更新/总是新增） | 1h |
| 7 | 跨 sheet 去重（同一笔记任何 sheet 已收 → 都算已收） | 0.5h |
| 8 | 跨设备同步配置（chrome.storage.sync） | 0.5h |
| | **小计** | **~10.5h** |

**Phase 2.0 不做**：批量收录、快捷键、iOS、多租户（留给 2.1/2.2）

### Phase 2.1 — 加柯希安（**先验证多人 OK 再扩展**）

🎯 目标：把 v2.0 改造成多租户，第一个测试用户是柯希安

| # | 功能 | 工时 |
|---|---|---|
| 1 | 扩展加「我的飞书表」配置项 | 1h |
| 2 | 后端 API 改造：所有接口加 user_token 鉴权，路由到对应表 | 3h |
| 3 | 用户管理：你（admin）能颁发 user_token | 1h |
| 4 | 柯希安自己的表全套搞起来（建 sheet、加 bot 协作者） | 1h |
| 5 | 联调 + bug 修 | 2h |
| | **小计** | **~8h** |

### Phase 2.2 — 团队 10 人扩展 + 高阶功能

🎯 目标：剩下 8 人加入 + 加批量收录、iOS、快捷键

| # | 功能 | 工时 |
|---|---|---|
| 1 | 批量收录小红书搜索页 | 4.5h |
| 2 | 快捷键 Cmd+Shift+X | 0.5h |
| 3 | iOS 捷径入口 + 端点适配 | 3h |
| 4 | 团队 8 人加入流程（脚本化加 bot 协作者） | 1h |
| 5 | 团队仪表盘（admin 视角看全员收录数） | 1h |
| | **小计** | **~10h** |

### 三阶段总工时：**约 28-29 小时**

---

## 5. 数据模型最终设计

### 用户的飞书表结构（每人一张表）

```
《XX 的小红书笔记库》
├── Sheet「起号」                  ← 固定，预建
├── Sheet「爆款」                  ← 固定，预建
├── Sheet「同行精选」              ← 用户自定义标签
├── Sheet「潜力款」                ← 用户自定义标签
└── Sheet「<其他标签>」            ← 按需建
```

### 每个 Sheet 的列结构（15 列，比 v2 多一列）

| 列 | 字段 | 来源 |
|---|---|---|
| A | 序号 | 自动 |
| B | 笔记链接 | 自动 |
| C | 状态 | 自动 |
| D | 标题 | 自动 |
| E | 封面图 | 自动 |
| F | 文案 | 自动 |
| G | 点赞 | 自动 |
| H | 收藏 | 自动 |
| I | 评论 | 自动 |
| J | 分享 | 自动 |
| K | 笔记 ID | 自动（去重键） |
| L | 采集时间 | 自动 |
| M | 来源 | 自动 |
| N | 我的备注 | 用户填 |
| **O** | **所有图（按顺序）** | **🆕 v3 新增**：除封面外的其他图都嵌入这里 |

### 服务端用户配置（config.json 重构）

```json
{
  "app_id": "cli_a90ee8a787225bd8",
  "app_secret": "...",
  "users": {
    "xiaoxiong": {
      "name": "王小熊",
      "auth_token": "xxx",
      "spreadsheet_token": "Ind1sh1YthdYlat3uEhc6d4Gn9f"
    },
    "kexian": {
      "name": "柯希安",
      "auth_token": "yyy",
      "spreadsheet_token": "<柯希安飞书表 token>"
    }
  }
}
```

---

## 6. API 接口契约（v2.0 升级版）

### 现有接口改造（加 user_token 路由）

| 接口 | 改造 |
|---|---|
| `GET /api/health` | 不变 |
| `GET /api/check?url=` | Header `X-Auth-Token` 路由到对应用户表，跨所有 sheet 查重 |
| `POST /api/collect` | body 加 `sheet_name`（默认"起号"），按用户路由 |
| `POST /api/collect-batch` | 同上 |

### 新增接口

| 接口 | 用途 |
|---|---|
| `GET /api/categories` | 返回当前用户的所有 sheet 名（用于 popup 下拉） |
| `POST /api/categories` | 创建新分类 sheet（管理页用）|
| `DELETE /api/categories/:name` | 解除关联（不删 sheet，只从扩展配置移除） |
| `GET /api/stats` | 返回 `{today, this_week, total}` |
| `GET /api/recent?limit=5` | 最近 N 条记录 |
| `GET /api/failures` | 失败列表（带 retry 按钮） |
| `POST /api/retry/:id` | 重试某条失败记录 |

### 鉴权升级

- 每个用户一个独立 `auth_token`（admin 颁发）
- 服务器根据 token 找到对应用户 → 找到 spreadsheet_token → 写到该用户表

---

## 7. 风险与对策

| 风险 | 级别 | 对策 |
|---|---|---|
| bot 自动建 sheet 权限不够 | 高 | Phase 2.0 第一步先实测；不够补 scope |
| 多用户并发写飞书 API 限流 | 中 | 服务端加令牌桶限流（每用户 10 req/min） |
| 用户表结构不一致（漏建 sheet）导致写入失败 | 高 | 扩展首次连接时自动检查 + 一键补齐 |
| 小红书前端改版导致采集失败 | 高 | 监控 + 失败列表反馈 |
| 服务器单点故障 | 中 | PM2 已自启，加每周备份 |
| auth_token 泄露 | 中 | 每人独立 token，泄露只影响一人；admin 能随时吊销 |

---

## 8. 验收标准（每阶段必跑）

### Phase 2.0 验收
- [ ] 扩展加新分类「测试标签」→ 飞书表自动出现「测试标签」sheet（带表头）
- [ ] popup 单选下拉显示「起号 / 爆款 / 测试标签」
- [ ] 选「起号」收笔记 → 数据进起号 sheet，O 列有图片
- [ ] 跨 sheet 去重生效：同笔记换分类不能重收
- [ ] popup 顶部「今天 N / 本周 N / 累计 N」准确
- [ ] 失败的笔记能从扩展里一键重试

### Phase 2.1 验收
- [ ] 柯希安加入 → 收的笔记进他自己的表，不进你的表
- [ ] 你看不到柯希安的数据（除非你看他的飞书表）
- [ ] 柯希安不带 token → 401 拒绝

### Phase 2.2 验收
- [ ] 团队 10 人各自能用
- [ ] 你能看团队全员收录总量
- [ ] iOS 上手机也能存

---

## 9. 实施前的最后一道确认

**你给我开干信号之前请检查：**

1. ☐ Assumptions A1-A5 都成立？
2. ☐ 分阶段顺序（Phase 2.0 → 2.1 → 2.2）你接受？
3. ☐ 工时预估（28-29h，分 3 次会话完成）你接受？
4. ☐ 飞书表加列（O 列「所有图」）你接受？
5. ☐ 关键词自动分类**不做**你接受？

全部 ✅ 后回「开干 Phase 2.0」，我立刻动手。
