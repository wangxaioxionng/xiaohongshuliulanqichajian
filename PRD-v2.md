# 小红书一键收录 v2 完整需求清单（PRD）

> 编制时间：2026-05-22 凌晨
> 操盘人：王小熊
> 目标：把当前 v1.0.3 升级为「分类管理 + 批量 + 历史看板」三件套

---

## 一、已实现的基线（v1.0.3）

| 模块 | 功能 |
|---|---|
| 单条采集 | 点扩展图标 → 弹窗 → 写备注（可选）→ 写入飞书表 |
| 右键菜单 | 小红书页面任意位置右键 → 「📥 收录这条小红书笔记到飞书」 |
| 自动重复检测 | 浏览已收录笔记，扩展图标自动 ✓ 徽标 |
| 当前页标题显示 | popup 顶部显示笔记标题（v1.0.3 新增） |
| 后端 API | `/api/health` `/api/check` `/api/collect` `/api/collect-batch` |
| 鉴权 | X-Auth-Token 内置在扩展，用户不用配 |
| 飞书写入 | bot 身份，写 14 列 + 嵌入封面图 |
| 进程管理 | PM2 自启（pm2-root.service enabled） |

---

## 二、本轮要做的需求（P0，全部要做）

### 需求 2-1：分类 = 飞书多 sheet 对应

#### 用户场景
看到一条好笔记时，能选「这条归到买点 sheet / 标题公式 sheet / 互动引导 sheet ...」。每个分类对应飞书表《小红书文案收集》里的一个独立工作表 tab。

#### 飞书侧准备
在《小红书文案收集》主表里，新增 7 个 sheet：

| Sheet 名 | sheet_id | 用途 |
|---|---|---|
| **总库** | `cfc98a`（已存在，重命名）| 默认 / 没选分类时存这里 |
| 买点 | 新建 | 笔记里挖出的产品卖点 |
| 标题公式 | 新建 | 学标题写法 |
| 开头钩子 | 新建 | 学第一句怎么钩住人 |
| 互动引导 | 新建 | 评论区/收藏诱导话术 |
| 封面设计 | 新建 | 看封面构图 |
| 竞品 | 新建 | 同品类对手 |
| 爆款 | 新建 | 高点赞参考 |

每个 sheet 都建好相同的 14 列表头 + 列宽 + 行高 + C 列下拉 + 冻结首行（用 lark-cli 脚本批量生成）。

#### 扩展侧设计
- popup 加「分类下拉」单选控件，默认选中"总库"
- 记忆「上次用过的分类」（chrome.storage.local）
- popup 底部加「⚙️ 管理分类」入口，进入新页面 `categories.html`
- 管理页：
  - 列出所有分类（名称 + 对应 sheet 名 + sheet_id）
  - 操作：增（输入新分类名 → 自动创建对应 sheet）/ 删（同时删 sheet 还是只解除关联？建议默认只解除关联，sheet 保留以防误删）/ 改（重命名分类 + 同步重命名 sheet）/ 排序
  - 重置按钮：回到默认 8 个分类
- 旧的"标签 chip"（买点/标题公式/...）从 popup 移除，统一并入「分类下拉」

#### 后端改造
- `/api/collect` body 加可选字段 `category_sheet_id`（默认用 config.json 里的 sheet_id）
- 内部根据 sheet_id 找对应 sheet 写入
- `/api/check` 改为查全表（跨 sheet 查重复）：bot 调 spreadsheet metainfo 拿所有 sheet_id 列表，逐个查
- 新增 `/api/sheets`：返回当前飞书表里所有 sheet 的列表（供管理页使用）
- 新增 `/api/sheets`（POST）：创建新 sheet（管理页加新分类时调用）

#### 去重策略
跨所有 sheet 去重（同一条笔记不管在哪个 sheet 都算已收录）。理由：避免同笔记在多个 sheet 重复出现。

#### 工作量
- 飞书 sheet 建立 + 表头格式：30 分钟（用脚本批量做）
- 后端 API 改造：1 小时
- 扩展 popup 改造：1 小时
- 管理页 categories.html：1 小时
- 测试 + 打包：30 分钟
- **小计：约 4 小时**

---

### 需求 2-2：批量收录小红书搜索页

#### 用户场景
在小红书电脑端搜「显脸小耳钉」→ 出现 30 条笔记的瀑布流 → 点扩展图标 → 「检测到 30 条笔记，一键全部收录」→ 自动批量入库。

#### 技术方案
- 在小红书页面注入 content script
- content script 识别当前页类型（搜索页 `/search_result` / 用户主页 `/user/profile` / 话题页 `/page/topics`）
- 解析 DOM 抽取所有可见笔记的链接（含 xsec_token）
- 通过 `chrome.tabs.sendMessage` 或 `chrome.runtime.sendMessage` 返回给 popup
- popup 显示「检测到 N 条，选分类后批量收录」
- 后端 `/api/collect-batch` 已存在，直接复用

#### 风险
- 小红书前端 DOM 结构会改版（每年 2-3 次），解析逻辑可能失效
- 一次性收录太多会触发风控（默认间隔 1.5s，30 条要 45 秒，体验差）
- 解决方案：分批后台异步收录，popup 关掉也能继续，完成后桌面通知

#### 工作量
- content script DOM 解析（4 种页面类型）：1.5 小时
- popup 批量 UI（进度条、可中断）：1 小时
- 后台异步队列（避免阻塞 popup）：1 小时
- 测试 + 调试 DOM 变化兜底：1 小时
- **小计：约 4.5 小时**

---

### 需求 2-3：历史记录 + 收录仪表盘

#### 用户场景
- 打开 popup 第一眼看到：「今天收了 3 条 / 本周 12 条 / 累计 67 条」
- 底部能展开「📜 最近收录」看 5 条最新（标题 + 时间 + 分类）
- 点列表里某条 → 直接跳到飞书表对应行

#### 数据来源
- 后端读飞书表 L 列（采集时间），按时间窗统计
- 后端读最近 5 行返回（标题、时间、分类）

#### 新增 API
- `GET /api/stats`：返回 `{today: N, this_week: N, total: N}`
- `GET /api/recent?limit=5`：返回最近 N 条 `[{title, sheet_name, row, time, url}]`

#### 扩展 UI
- popup 顶部「📊 今天 3 / 本周 12 / 累计 67」（带🔄手动刷新）
- popup 底部可折叠区「📜 最近收录」（展开显示 5 条 mini 卡片）
- 缓存：每次打开 popup 调 API 拉数据（带 10 秒缓存避免频繁请求）

#### 工作量
- 后端 stats + recent API：45 分钟
- popup UI（顶部数字 + 底部列表 + 折叠交互）：1 小时
- 测试：15 分钟
- **小计：约 2 小时**

---

## 三、暂不做（P1，下一批可选）

| 编号 | 功能 | 工时 |
|---|---|---|
| 3-1 | 笔记预览：采集前先看标题/三数 | 1.5h |
| 3-2 | 快捷键收录（Cmd+Shift+X） | 30min |
| 3-3 | iOS 捷径入口 | 半天 |
| 3-4 | 失败列表 + 一键重试 | 1h |
| 3-5 | 跨设备同步配置（chrome.storage.sync） | 30min |
| 3-6 | AI 拆解文案结构（开头钩子/买点/CTA） | 2-3h |
| 3-7 | 爆款自动分级（按点赞数打标） | 30min |
| 3-8 | 数据导出 Excel | 30min |
| 3-9 | 飞书群机器人入口 | 半天 |
| 3-10 | 关键词聚类报告 | 半天 |

---

## 四、本轮实施顺序

```
[需求 2-1 分类管理]
   ├─ Step 1: lark-cli 脚本建 7 个新 sheet + 重命名 cfc98a 为「总库」
   ├─ Step 2: 后端 API 支持 sheet_id 参数 + /api/sheets 接口
   ├─ Step 3: 扩展 popup 单选下拉
   └─ Step 4: 管理页 categories.html

[需求 2-2 批量收录]
   ├─ Step 5: content script DOM 解析（先做搜索页）
   ├─ Step 6: popup 检测当前页类型 + 批量 UI
   └─ Step 7: 后台异步队列

[需求 2-3 历史 + 仪表盘]
   ├─ Step 8: 后端 stats + recent API
   ├─ Step 9: popup 顶部仪表盘
   └─ Step 10: popup 底部最近列表

[联调 + 打包]
   ├─ Step 11: 端到端测试三个新功能
   ├─ Step 12: 打包 v2.0.0 zip
   └─ Step 13: 写中文使用手册
```

**总工时：约 10.5 小时**

---

## 五、不在范围内的事

- 不改飞书表的列结构（仍是 14 列 A-N，避免破坏现有数据）
- 不动其他业务（bookboy / gold-rush / xiaoxiong-workbench 完全不碰）
- 不引入新的服务器中间件（不要 Redis / MQ / DB，保持 stateless API）
- 不为 Phase 2（飞书群机器人）做提前准备工作

---

## 六、风险与对策

| 风险 | 概率 | 影响 | 对策 |
|---|---|---|---|
| 小红书前端 DOM 改版 | 高 | 批量收录失效 | 后端有版本号 + 失败提示「联系开发者」 |
| 飞书 sheet 数量上限 | 低 | 分类太多放不下 | 飞书单表 200 sheet 上限够用 |
| bot 创建 sheet 权限不够 | 中 | 管理页创建分类失败 | 测试后补充 scope `sheets:spreadsheet:write_only` 应当够；不够再补 |
| 跨 sheet 查重导致 /api/check 变慢 | 中 | 扩展徽标响应慢 | 给 metainfo 加内存缓存（5 分钟） |

---

## 七、验收清单

每个需求做完后必须跑通：

### 需求 2-1
- [ ] 飞书表里 8 个 sheet 都建好 + 表头一致
- [ ] popup 单选下拉显示 8 个分类
- [ ] 选「买点」→ 收录 → 数据进「买点」sheet
- [ ] 同笔记换分类再收 → 跨 sheet 去重，跳过
- [ ] 管理页能新增/删除/重命名分类（同步飞书）

### 需求 2-2
- [ ] 小红书搜索结果页能识别出所有笔记链接
- [ ] popup 显示「检测到 N 条」按钮
- [ ] 批量收录可中断
- [ ] 收完后桌面通知

### 需求 2-3
- [ ] popup 顶部显示三个数字（今天/本周/累计）
- [ ] 底部能展开最近 5 条
- [ ] 点某条跳到飞书表对应行

---

**等小熊确认本 PRD 范围正确后开干。**
