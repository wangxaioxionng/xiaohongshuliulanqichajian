# 小红书一键收录 · 给你的使用手册

> 这份文档是给团队成员看的。如果你只是想把好的小红书笔记一键存到自己飞书表里，看这一份就够。

---

## 你能用它做什么

- 在 Chrome 浏览器看小红书笔记 → **点扩展图标** → 一键存到你自己的飞书表
- 笔记的**标题、文案、点赞收藏评论数、封面图**都自动入库
- 看到已经存过的笔记，扩展图标自动显示 `✓` 提示
- 顶部能看到「今天存了 X 条 / 本周 X 条 / 累计 X 条」

---

## 安装（5 分钟，第一次用）

### 1️⃣ 拿到你的「钥匙」（Auth Token）

找王小熊要：
- 一个 **Auth Token**（一串随机字符）
- 一份**扩展安装包**（`xhs-extension-vX.X.X.zip`）

### 2️⃣ 装扩展到 Chrome

1. 双击 zip 解压，得到一个 `extension` 文件夹
2. Chrome 地址栏访问 `chrome://extensions`
3. 右上角打开「**开发者模式**」开关
4. 点「**加载已解压的扩展程序**」按钮
5. 选刚才解压出来的 `extension` 文件夹
6. 扩展栏会出现一个红底白「红」字图标 ✅

### 3️⃣ 填你的 Token

1. 点扩展图标
2. 在弹出窗口里，点右上角的 ⚙️
3. 进入设置页：
   - **API Endpoint** 保持默认（`http://14.22.112.147:8866`）
   - **Auth Token** 粘贴王小熊给你的那串字符
4. 点「🔌 测试连接」→ 应显示「✅ 连接成功」
5. 点「💾 保存」

完成！现在可以用了。

---

## 怎么用

### 收一条笔记

1. 在 Chrome 打开任意小红书笔记
2. 点扩展图标（右上角红色「红」字）
3. 弹出小窗口里：
   - 顶部显示这条笔记的**标题**
   - **分类下拉**选「起号 / 爆款 / 同行精选 / …」中的一个
   - 可选：写**备注**（比如「开头钩子值得学」）
4. 点「📥 收录到飞书」
5. 3-5 秒后显示 `✅ 已收录到第 X 行`，同时显示点赞收藏评论数
6. 打开你的飞书表，能看到这条笔记的所有信息（封面图都嵌进去了）

### 看看自己存了多少

打开扩展图标，**顶部红色卡片**：
- **今天**：今天存了几条
- **本周**：本周存了几条
- **累计**：总共存了几条
- 如果有失败的，会显示「失败 N」红色数字，点击展开能看到失败列表 + 重试按钮

### 看「最近存的」

popup 底部「📜 最近收录」点击展开，能看到最近 5 条笔记的标题 + 分类 + 时间。点任一条直接跳到那条笔记的飞书表位置。

### 自己新建一个分类

popup 里点「分类下拉」旁边的 **+** 按钮 → 输入新分类名（比如「化妆类」）→ 飞书表里自动创建一个叫「化妆类」的 sheet。

### 右键也能存

任意小红书页面右键 → 选「📥 收录这条小红书笔记到飞书」（不弹窗口，直接收，用上次选的分类）。

---

## 常见问题

### Q：怎么知道这条笔记已经存过了？
扩展图标右下角出现绿色 `✓` 徽标 = 已存过。也可以打开 popup，顶部黄色提示会告诉你存在哪个分类、第几行。

### Q：我想更新某条笔记的最新点赞数怎么办？
点扩展图标 → ⚙️ → 找到「重复笔记怎么处理？」→ 选「**更新 — 更新点赞收藏评论数为最新**」→ 保存。

之后再收同一条笔记时，会自动更新数据，不会新增行。

### Q：「连接失败」怎么办？
- 浏览器访问 `http://14.22.112.147:8866/api/health`，应该看到 `{"ok":true,...}`
- 看不到说明服务器挂了，找王小熊
- 看得到但扩展还是连不上 → 检查 ⚙️ 设置页里 Token 有没有填错

### Q：哪些数据是私密的？
你的数据只写到**你自己的飞书表**，别人看不到（除非你主动分享给别人）。其他同事用同一个服务器但有自己独立的 token + 自己的飞书表。

### Q：怎么删除某条笔记？
直接去你的飞书表里删除那行就行，扩展不需要操作。

---

## 给王小熊的求助清单

- 我没收到 Token：找你要
- Token 不能用：你可能要重启服务（`pm2 restart xhs-collect-api`）或者帮我重新生成 token（`python admin.py rotate --id myid`）
- 我想换飞书表：你帮我改 `config.json`
- 扩展坏了：你重新打包 zip 发我

---

## 最后一句

**这工具不会上传任何东西到第三方**。所有数据走「你的 Chrome → 王小熊公司服务器 → 你自己的飞书表」，全程内网。
