# 更新日志

本项目所有显著变更都会记录在这里。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：MAJOR.MINOR.PATCH

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [4.9.1] - 2026-07-31

### 修复
- 重做千帆商品笔记显示逻辑：扩展主工作区到右侧安全边距，不再把14列压进固定宽度。
- 指标列最低84px，保留完整表头和双行环比可读性，并支持完整恢复官方布局。
- 插件弹窗的页面和内容宽度固定为360px，修复右侧大块空白。
- 补充页面外壳、表格恢复、空状态、错误状态和弹窗宽度回归测试。

## [4.9.0] - 2026-07-31

### 新增
- 千帆「笔记数据-商品笔记」页面新增“全量显示 / 恢复官方布局”开关
- 当前已加载的 14 列可压缩到同一屏，指标表头使用紧凑业务名称并保留完整名称提示
- 仅调整当前标签页展示，不调用后端、不修改店铺数据；补充页面状态、自动重渲染和回归测试

## [4.8.6] - 2026-06-07

### 修复
- 账号全采集表新增「点赞、收藏、评论、分享」四列，位置在笔记链接后面
- 旧全采集页签会自动插入四列，避免图片列和状态列错位
- Rnote 图文详情返回的互动数据会写入飞书，支持对旧账号页签按链接回填四个字段

## [4.8.5] - 2026-06-07

### 修复
- 修复小红书商品详情 template_data 新结构下单品销量无法写入「已售数」的问题
- 现在可从 priceH5.itemAnalysisDataText 读取「已售38」并解析为 38
- 新增真实接口结构回归测试，避免后续字段再漏

## [4.8.4] - 2026-06-07

### 变更
- 账号全采集新增 Rnote API provider，可通过服务端配置切换新旧 API
- Rnote 路线会先获取账号笔记列表，再逐篇调用图文详情接口，保留失败落表和自动补采
- Rnote API Key 只放服务端 config.json，不写入浏览器插件安装包

## [4.8.3] - 2026-06-07

### 变更
- 账号主页卡片新增统一备注输入，加入对标账号库时自动带入备注
- 账号全采集任务会携带页面备注，方便后续链路保留上下文
- 爆款跟品和潜力店铺账号库去掉品类、风格字段，备注前移到 J 列，状态前移到 L 列
- 打包规则继续排除开发缓存和系统隐藏文件

## [4.8.2] - 2026-06-04

### 修复
- 修复小红书商品详情接口 template_data 新结构下店铺 ID 识别失败
- 现在可从 sellerH5、bottomBarMainH5、graphicDetailsV4、headerBarMainV2 等位置提取店铺信息
- 针对商品链接 6a1c09fd445b6f00151cd59f 补充回归测试

## [4.8.1] - 2026-06-04

### 修复
- 普通文案/爆款文案采集新增图片数量、全部图片下载链接、图片1-图片20字段
- 普通笔记采集会最多嵌入20张图片，不再只保存封面图
- 保留A-O旧字段位置，避免影响去重、失败重试和看板统计

## [4.8.0] - 2026-06-01

### 新增
- 店铺商品页签支持写入「店铺商品提取」专用表
- 移除弹窗第三页配置设置，保留笔记提取和店铺商品两页
- 分类下拉排除工具专用表，并修复最近收录加载失败无提示

## [4.7.3] - 2026-06-01

### 修复
- 账号全采集失败的笔记链接会先保存到同一个「XX全采集」飞书表，不再只停留在弹窗状态里。
- 第一轮采集结束后会自动补采失败行；补采成功会覆盖原失败行，补采仍失败则保留失败原因。
- 插件进度新增「补采失败笔记」阶段，并显示失败已落表、补采成功和仍失败数量。

### 质检
- 新增「主页采集失败落表补采」用例，验证失败行落表后可被补采成功结果覆盖。

## [4.7.2] - 2026-06-01

### 修复
- 账号全采集逐篇调用哼哼猫单篇 API 时，单条失败会自动重试，减少接口临时波动造成的漏采。
- 账号全采集默认调用间隔从 1 秒调整为 2.5 秒，降低 400 篇连续采集时的接口压力。
- 账号全采集任务状态保留更多失败明细，便于判断失败原因和后续补采。

### 质检
- 新增「主页采集临时失败重试」用例，验证第一次 API 失败、第二次成功时不会被误计为失败。

## [4.7.1] - 2026-05-31

### 修复
- 账号主页信息优先读取小红书页面内部状态，页面文字延迟加载时仍能抓账号名、小红书号、粉丝数、获赞收藏和笔记数。
- 账号全采集增强图片保存：保留图片请求头，提升小红书图片下载和嵌入飞书表的成功率。

### 质检
- 发布前质检新增「账号主页状态兜底」和「主页采集图片嵌入」用例。

## [4.7.0] - 2026-05-30

### 调整
- 账号全采集改为“双保险”：先从小红书页面状态提取带 `xsec_token` 的完整笔记链接，再逐篇调用哼哼猫单篇接口。
- 账号主页只拿到短链接时，不再直接判失败；后端会尝试哼哼猫主页批量接口兜底，并明确显示失败原因。

### 修复
- 修复账号全采集状态仍显示旧的“逐篇 API / 慢速翻页”阶段，导致用户看不清任务实际卡点的问题。
- 修复小红书页面里明明有 `__INITIAL_STATE__` 数据，但插件只读卡片短链接，导致单篇 API 链接不完整的问题。

### 质检
- 发布前质检新增「主页采集路由模式」用例，验证有完整笔记链接时走单篇 API、没有完整链接时才走主页批量接口兜底。

## [4.6.9] - 2026-05-30

### 修复
- 修复账号主页卡片抓不到账号名、粉丝数、获赞收藏等信息时缺少兜底解析的问题。
- 账号名提取新增页面标题和正文文本兜底，页面结构变化时仍尽量识别账号名。

### 质检
- 新增发布前质检脚本：覆盖页面识别、账号信息、笔记浮层、主页链接质量、断点续采、脚本语法和打包敏感文件扫描。

## [4.6.8] - 2026-05-30

### 修复
- 修复 Atlas/多窗口场景下插件可能拿错当前标签页，导致小红书页面被误判为“当前不是小红书笔记页”的问题。
- 当前窗口识别失败时，会继续尝试最后聚焦窗口和其他激活标签页，优先匹配小红书页面。

## [4.6.7] - 2026-05-30

### 修复
- 修复小红书搜索页/主页里点开笔记浮层后，插件仍提示“当前不是小红书笔记页”的问题
- 单篇收集会优先识别当前页面里实际打开的笔记链接，再提交 API，避免把主页链接或搜索页链接当成笔记提交
- 账号主页没有打开笔记时仍显示账号采集卡片；打开某篇笔记时则切回单篇收集入口

## [4.6.6] - 2026-05-30

### 修复
- 账号全采集提交 API 前先筛完整笔记链接：缺少 `xsec_token` 的短链接不再直接提交，避免出现“找到 13 条但 API 13 条全失败”
- 弹窗新增“完整链接/短链接”数量展示；如果全是短链接，会直接提示“缺少 xsec_token”，并保留已找到链接用于后续补采
- 主页链接提取优先读取浏览器解析后的完整链接，并允许已保存短链接被后续完整链接替换
- 进一步加固账号主页粉丝数识别，优先按“关注/粉丝/获赞与收藏”的整段结构解析

## [4.6.5] - 2026-05-30

### 修复
- 修复小红书账号主页粉丝数识别错误：`5388 粉丝 7.1万 获赞与收藏` 这类页面不再把获赞收藏误当粉丝
- 当前账号卡片新增“获赞收藏”显示，便于一眼核对粉丝数和获赞收藏是否串位

## [4.6.4] - 2026-05-30

### 新增
- 账号全采集新增断点保存：主页提取链接时每 80 条保存一次，重开同账号会先恢复已保存链接再继续补采
- 弹窗进度新增“已保存链接数”和“已落表数量”，中断后能判断保留到了哪一步
- 后端改为 API 成功一篇就立刻写入飞书，避免长任务中途失败后丢掉已成功采集的部分

## [4.6.3] - 2026-05-30

### 修复
- 账号全采集长任务从 popup 弹窗迁到 background + 页面任务，避免弹窗关闭后链路中断
- popup 可恢复后台采集进度，重新打开也能看到当前跑到哪一步
- 后端任务新增 API 提取、飞书建表、飞书写入阶段信息，失败时返回具体断点
- 账号全采集完成后返回目标飞书 sheet 链接，方便直接打开验收

## [4.6.2] - 2026-05-29

### 改进
- 账号全采集新增全过程状态显示
- 主页翻页阶段会显示已找到链接数、滚动次数、风控暂停状态
- 后台逐篇采集阶段会显示已处理、成功、失败数量和部分失败原因
- 增加进度条，避免用户误以为插件卡死

## [4.6.1] - 2026-05-29

### 调整
- 账号全采集的主页链接提取改为慢速分批滚动
- 每次滚动随机停 1.2-2.2 秒，每批新增约 25 个链接或滚动 12 次后额外停 4-7 秒
- 前 400 篇上限不变，降低一次性快速滚动带来的风控风险

## [4.6.0] - 2026-05-29

### 新增
- 账号全采集改为「主页提取链接 + 单篇 API 逐篇采集」
- 插件会在账号主页自动滚动，最多提取前 400 篇笔记完整链接
- 后端新增账号采集后台任务，避免 400 篇采集时请求超时
- 采集进度显示已处理、成功、失败数量；关闭 popup 后后台任务仍继续跑

### 修复
- 修复 v4.5.0 直接把小红书主页传给哼哼猫主页 API 导致「暂不支持此链接」的问题
- 单篇采集保留 `xsec_token` 完整链接，避免只传笔记 ID 导致单篇 API 失败

## [4.5.0] - 2026-05-29

### 新增
- 账号主页新增「采集这个账号图文」入口
- 支持按账号采集标题、图文文案、话题标签和全部图片下载链接
- 自动写入「账号名全采集」sheet，保留封面图、全部图片下载链接和图片 1 / 图片 2 等独立列
- 大量采集走服务端第三方 API，不再用浏览器逐页访问小红书页面

### 后端 + 前端
- 新增 `POST /api/profile-collect` 接口
- 新增服务端配置项 `meowload_api_key`、`profile_collect_max_items`、`profile_collect_delay`
- 扩展版本升级到 v4.5.0

## [4.4.2] - 2026-05-29

### 变更
- 加入对标账号库弹窗的备注字段上移到账号名下方
- 打开弹窗第一屏即可填写备注，不用滚动到底部

## [4.4.1] - 2026-05-29

### 变更
- 图文对标更名为爆款图，并改为笔记收录表结构
- 爆款图文更名为爆款文案
- 爆款图支持一条笔记保存全部图片
- 账号库移除图文对标，只保留潜力店铺和爆款跟品

## [4.4.0] - 2026-05-25

### 新增（重磅）
- 📋 **对标账号库**：在小红书账号主页点扩展，可一键加入「图文对标 / 潜力店铺 / 爆款跟品」3 类账号库
- 🔍 自动抓取账号主页 8 字段：账号名 / 主页URL / 小红书号 / 笔记数 / 粉丝数 / 获赞收藏 / IP属地 / 简介
- 🏷️ 表单内多选打标签：品类（8 选 N）+ 风格（7 选 N），按业务方向区分账号
- 🛡️ 跨 3 sheet 自动去重：同一账号 URL 已在某 sheet 则提示「已在「XX」第 N 行」，不重复入库
- ⚙️ 首次使用时自动建表 + 套样式（深蓝表头 + 淡蓝灰斑马纹），无需手动配置
- 后端新增 `POST /api/account-lib/add` + `GET /api/account-lib/meta` endpoint
- 扩展端新增 popup 账号库卡片 + 完整表单 modal（不依赖现有 collect 流程）

### 改进
- `manifest.json` 加 `scripting` 权限，用于在小红书账号主页 executeScript 抓字段
- URL 归一化逻辑（去 query / 大小写无关 / http↔https 等价）保证去重精确

### 修复
- 无

### 后端 + 前端
- 这次同时改了后端和扩展，**minor 版本号 bump**（4.3.7 → 4.4.0）
- 扩展用户需要更新到 v4.4.0 才能用新功能（旧版本不受影响，老笔记收录流程不变）

## [4.3.7] - 2026-05-24

### 改进
- 🎨 **飞书表样式升级**：表头从浅灰底深灰字 → 深蓝底 (`#1F4E79`) + 白字 + 14pt 加粗居中，看着更专业
- 🦓 斑马纹底色从 `#F9FAFB`（几乎看不见）→ `#F3F6FB` 淡蓝灰，行间区分更清晰
- ⚙️ 行高保持 32px 紧凑模式不变，单屏可显示笔记数不受影响
- 影响范围：**仅未来新建 sheet 自动套新样式**（onboarding 首次绑表 / 老用户新增分类）
- 已建好的老 sheet **保持原样不动**（按王小熊决定，不跑 `backfill_sheet_style.py`）

### 修复
- 🐛 **表头样式范围 bug**：旧代码 `_set_cell_style("A1:N1", ...)`，但 v4.3.6 B037 新增 O 列「话题标签」后，表头实际写到 A1:O1 — O 列表头一直无样式。改为 `f"A1:{LAST_COL}1"`，未来再加列不用记得改

### 后端 only
- 此版本 **不动扩展前端代码**（manifest 同步 bump 仅做版本号对齐），扩展用户无需重新安装/更新
- 任何版本扩展（含 v4.3.6 及更早）新建 sheet 时都会自动用上新样式（样式逻辑全在服务端 `lark_writer.py:setup_sheet_template()`）
- patch 级静默更新（按宪法 patch 不弹窗）

## [4.3.6] - 2026-05-23

### 新增
- 🏷️ **B037：hashtag 拆到独立 O 列**：飞书表新增「话题标签」列，文案里的 `#xxx[话题]#` 自动拆出来，F 列只剩纯文案
- 新增 `extract_hashtags()` 工具函数（正则 `#[^#\n]+?#`）
- `build_row` / `app.py` update 路径 / `backfill_extract_hashtags.py` 三路径全覆盖
- 王小熊 13/15 行已 backfill（2 行无 hashtag 跳过）
- 数据源用飞书 F 列当前内容（不依赖不完整的 desc_backup）

## [4.3.5] - 2026-05-23

### 新增
- 🔗 **B036：支持飞书 Wiki URL 绑表**：之前只认 `/sheets/`，现在 `/wiki/` 也能直接粘贴
- 后端新增 `resolve_wiki_to_sheet_token()` 调飞书 `wiki/v2/spaces/get_node` API 解析
- `/api/permissions/check` 和 `/auth/bind-sheet` 都用 `resolve_to_spreadsheet_token()` smart resolve
- 前端 `parseSpreadsheetToken()` 同时识别 `/sheets/{token}` 和 `/wiki/{wiki_token}`
- 真实数据验证：朋友的 Wiki URL `LAPiwUgATic7gtkujJoctaJPnoc` 解析成 `USWAsgtpOhAi1kttNZLcJQKfncb` 成功

## [4.3.4] - 2026-05-23

### 修复
- 📖 **B034 v6 分隔符删空格**：v5 单空格仍有视觉断开 → v6 直接删除 `\n` 不加任何分隔符，飞书表呈现普通连续文案（依然单行不撑高）
- ✅ 王小熊 9 行从 desc_backup 表原文重做（原文仍完整保留可任意还原）

## [4.3.3] - 2026-05-23

### 修复
- 📖 **B034 v5 分隔符改单空格**：v4 用 ` · ` 中点用户反馈太密影响阅读 → v5 改单空格，飞书表 F 列像标题一样横向连续阅读
- 🔁 **rerun_flatten_from_backup.py**：新工具脚本，从 desc_backup 表取原文重做替换（避免基于已替换内容做二次替换）
- ✅ 王小熊 9/9 行已用新分隔符重写，desc_backup 原文仍然完整保留

## [4.3.2] - 2026-05-23

### 修复
- 🎯 **B034 v4 终极修复**：飞书表行高被长文案撑高的根因 = cellValue 含 `\n`。改成写入前 `\n` → ` · ` 替换，飞书自动单行渲染，行高自然统一
- 🛡️ **零数据丢失**：新增 SQLite `desc_backup` 表，backfill 前每行原文落库（按 row_id 精确还原，不靠字符串反向 replace）
- 🔄 **覆盖 update 路径**：`app.py` 重复笔记 update 分支也用 `flatten_desc()`，新数据写入都不再撑高行

### 工具
- `server/backfill_flatten_desc.py` 一次性脚本：王小熊 9/9 行成功 backfill + backup

## [4.3.1] - 2026-05-23

### 修复
- 🐛 **B034 v3 闭环修复**：飞书表新写入数据时 `write_record` 不再强制 260px 行高（之前会覆盖 backfill 的 32px 设置，导致新收录笔记行高回归不一致）
- 🐛 **B035 dup-hint 行号可点击**：「已在「起号图文」第 N 行」整段加 `cursor:pointer` + 点状下划线，点击直接跳飞书表（不只是底部按钮）
- 🎨 **B034 v2 全表行高统一**：飞书表所有数据行（含旧封面图行）一律压成 32px，长文案双击单元格看全文

## [4.3.0] - 2026-05-23

### 新增
- ⌨️ **Cmd+Shift+X 一键收录快捷键**（B032）：在小红书页按快捷键直接收录，用上次选的分类，无需打开 popup
- 🔔 **Chrome 原生通知反馈**（B030）：popup 关闭后 API 完成会弹系统通知（manifest 加 `notifications` 权限）
- 🎬 **onboarding 屏 4 加「打开示例笔记演练」按钮**（B027）：新员工配置完成后引导第一次试用
- 📝 **onboarding 屏 3 加"新建飞书表"前置教学**（B024）：先教新建表，再教加文档应用
- ✏️ **重复笔记提示加「在飞书表里改这行」「跳过」按钮**（B029）：不再只说事实，给出下一步动作

### UI 改进
- 🐛 修复 popup label 错字「收到」→「收录到」（B023）
- 🔍 onboarding 屏 1.5 显示具体企业名「武汉眸佑商贸有限公司」（B025）
- 🔗 onboarding 屏 3 URL placeholder 改成真实域名 `acnl9ctl8qlc.feishu.cn`（B026）
- ➕ popup「+」加号按钮放大（88×36px + 「+ 新分类」文字 + hover 红底）（B028）
- 📜 popup「最近收录」默认展开（B031）
- ✨ popup 首次加载 skeleton shimmer 占位（B033）

## [4.2.0] - 2026-05-23

### 重大体验改进
- 🎯 **onboarding 第 3 屏全面重写**：教用户「⋯ → 更多 → 添加文档应用 → 小红书收录助手」（之前的"加协作者"路径是错的，飞书新版权限协作者≠写权限）
- 🔍 **新增「检测 bot 权限」预检按钮**：绑表前必须先通过权限检测，否则"绑定+建分类"主按钮 disabled
- 🛡️ **绑表流程严格化**：建分类失败不再被 `try/except: pass` 吞掉，返回 502 + 精准错误文案教用户修
- ✅ **完成屏前最终自检**：调 `/api/me` + `/api/sheets`，确认至少 7 个分类才放行进入「配置完成」
- 🎨 **飞书表格美化**：表头加粗+浅灰底+居中、数据行斑马纹、细边框、双冻结（首行+首列）、合理列宽
- 📜 **help.html 新增「添加文档应用」教程**（含 4 张占位图待王小熊补真截图）

### 新增 API
- `GET /api/permissions/check?spreadsheet_token=xxx`：权限自检（读+写）

### 安全 hardening
- 🔐 **激活码并发原子化**（单条 UPDATE WHERE used_by IS NULL），杜绝双激活漏洞（并发测试通过）
- 🔐 **OAuth state nonce 防 CSRF**：SQLite 持久化 nonce，TTL 10 分钟，进程重启/多 worker 也安全
- 🔐 **OAuth 错误页 XSS 修复**：错误码白名单 + `html.escape` 双重保险
- 🔐 **CORS Allow-Headers 加 `Authorization`**：v4 OAuth 不再靠 Chrome 扩展 host_permissions 绕过
- 🛡️ **deploy.sh 加 `--exclude data.db`** 等，防止部署覆盖生产数据库

### 修复
- 🐛 修复 PM2 ASGI Exception 日志噪音根因诊断（v3.1 遗留 TOKEN_TO_USER NameError，当前代码已无）

### 工具改进
- 🛠️ `bump-version.sh` 加 `--force` 参数：默认不删旧产物，加 --force 才把旧版本移到 `dist/.trash/<ts>/` 而非真删
- 🛠️ `bump-version.sh` 修 zip 路径 bug（之前会带 `dist/` 前缀）

### 新增 Bug 记录
- B017（P0）已实现：onboarding 绑表权限严格化
- B018（P1）已实现：`/api/permissions/check` 权限自检
- B019（P1）已实现：飞书表格美化
- B020（P1）已立：OAuth HTTPS 升级 + 域名（推迟，需用户配置域名）

## [4.1.0] - 2026-05-23

### 新增
- 🎉 「更新日志」面板：popup 右上角加铃铛，新版本上线自动弹窗
- 🔧 `bump-version.sh` 一键发版脚本：自动 bump manifest + 写 CHANGELOG + 打 zip
- 🐛 修复 popup「+」加号按钮无响应（Chrome MV3 禁用 window.prompt）

### 修复
- 🛡️ `server/deploy.sh` 加 `--exclude data.db` 等，防止部署覆盖生产数据库

### 文档
- 📝 修正 CLAUDE.md 虚假记录（v4.0.0.zip 实际不存在）

## [4.0.0] - 2026-05-22

### 重大变更
- 🚀 OAuth 自助登录架构上线（替代 v3.1 的 admin 手动发 token）
- 🔐 JWT 鉴权 + 飞书扫码 + 一次性激活码
- 🏢 新飞书企业 + 新 Bot「小红书收录助手」

### 新增
- 📋 onboarding 4 屏流程：扫码 → 激活 → 绑表 → 完成
- 📊 自动初始化 7 个默认分类 sheet
- 🔄 老用户重扫码自动恢复（无需重新激活/绑表）

## [3.1.0] - 2026-05-21

### 新增
- 👥 多租户支持（不同员工独立绑表）
- 🔍 Codex 反对派代码审计机制

## [2.3.2] - 2026-05-21 及更早

历史版本，省略详细记录。
