{
  "name": "xhs-promo-price-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "小红书千帆单品特价页面的促销价批量填写助手",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:coverage": "node --experimental-test-coverage --test-coverage-include=price-core.js --test-coverage-lines=90 --test-coverage-functions=90 --test-coverage-branches=85 --test tests/*.test.mjs",
    "check": "node --check price-core.js && node --check content.js && npm run test:coverage"
  }
}
