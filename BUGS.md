# 小红书一键收录扩展 · Bug 与改进清单

**最后更新**：2026-05-23（新增 B034 飞书表长文案撑高修复）
**维护者**：王小熊（PO） + Claude（记录员）
**交付对象**：开发同事
**当前版本**：v4.9.2（dist 目录），extension 源码 v 持续迭代中

---

## 使用说明

- 本清单为"开发可直接照着改、照着验收"的工单格式
- 每条 bug 包含：ID / 优先级 / 状态 / 复现路径 / 涉及文件 / 现状 / 期望 / 验收标准
- 「涉及文件」字段填写约定：
  - **整页/整文件重做** → 只写文件路径（不需要行号，因为整个文件都会被替换）
  - **修改具体函数/具体段落** → 写文件路径 + 行号范围，如 `popup.js:120-145`
  - **新增功能（新文件）** → 写"新增 `path/to/newfile.js`"
- 状态流转：`待修复` → `进行中` → `已修复（待验收）` → `已关闭`
- 优先级定义：
  - **P0**：影响核心功能可用性（采集失败、数据丢失、安全漏洞）
  - **P1**：影响用户体验或非核心功能（UI 丑、提示不清、流程卡顿）
  - **P2**：优化项（性能、代码质量、未来增强）

---

## 待修复清单

### B041 · 千帆宽屏模式整体偏右，左右留白不对称

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | 已修复（待王小熊真实页面验收）— v4.9.2 |
| **提出时间** | 2026-07-31 |
| **涉及文件** | `extension/qianfan_full_table.js` / `extension/popup.js` / `tests/verify_qianfan_full_table.py` |
| **复现路径** | 千帆「笔记数据-商品笔记」→ 打开扩展 → 开启宽屏分析 |

**现状描述**：
- v4.9.1 已解决 14 列可读性和插件弹窗异常变宽，但只扩展了主内容区，未同步调整左侧菜单。
- 真实页面中「左侧菜单 + 主内容」整体明显偏右，浏览器左侧留白远大于右侧留白。

**期望效果**：
- 保持 v4.9.1 的工作区宽度、指标列宽和字号不变。
- 把左侧菜单与主内容作为一个整体居中，浏览器左右留白接近一致。
- 点击「恢复官方布局」后，同时恢复菜单和主内容的官方位置。

**验收标准**：
1. 同一视口下，宽屏模式的 14 列宽度与 v4.9.1 一致。
2. 页面整体左右留白差不超过 2px。
3. 左侧菜单与主内容不重叠，筛选、排序、分页和详情入口位置正常。
4. 恢复官方布局后，菜单与主内容的原始内联边距全部还原。

**修复与验证记录**：
- 使用同一份千帆真实页面结构和 2341px 视口回归：左侧菜单与主内容同时向左移动，页面左右留白均为 253.5px，差值为 0。
- 主内容工作区继续保持 1692px，笔记表格继续保持 1628px；14 列完整显示，`clientWidth` 与 `scrollWidth` 均为 1628px。
- 恢复后菜单左边距回到 482.5px，主内容左右边距回到 624.5px / 482.5px，官方表格横向滚动宽度回到 2920px。
- 离线页面副本本身有 1 条既存监听器报错；开启本功能后没有新增报错，且补充了监听器异常容错测试。

---

### B039 · 千帆“全量显示”把 14 列压进固定宽度，真实数据不可读

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | 已修复（待王小熊真实页面验收）— v4.9.1 |
| **提出时间** | 2026-07-31 |
| **涉及文件** | `extension/qianfan_full_table.js` / `extension/popup.html` / `extension/popup.js` / `tests/verify_qianfan_full_table.py` |
| **复现路径** | 千帆「笔记数据-商品笔记」→ 打开扩展 → 开启全量显示 → 打开「对比上周期」 |

**现状描述**：
- v4.9.0 只把 14 列压缩进千帆原有的固定宽度卡片，没有利用浏览器右侧空白。
- 指标列最窄只有 56px；开启「对比上周期」后，本期值和环比值互相挤压，表头也无法正常阅读。
- 旧验收只检查“没有横向滚动”，没有检查字号、表头和环比数据是否可读。

**期望效果**：
- 改为“宽屏分析模式”：扩展千帆主内容外壳到浏览器可用宽度，再按可读下限分配列宽。
- 保留左侧菜单、筛选、排序、分页和笔记详情；支持一键恢复官方布局。
- 页面结构变化或刷新后能重新应用，恢复时完整还原原有内联样式。

**验收标准**：
1. 用户当前宽屏视口下，主内容区利用右侧空白，右侧安全边距约 24px。
2. 14 列全部显示，指标列不低于 84px，表头允许两行展示且不重叠。
3. 开启「对比上周期」后，本期值与红绿环比值可独立阅读。
4. 恢复官方布局后，页面外壳、表格列宽和表头全部还原。

**修复与验证记录**：
- 页面外壳右侧边距固定为 24px，保留原来的左侧位置；测试快照中工作区从 1234px 扩到 1692px。
- 14 列在 1628px 表格可视区内完整显示，10 个指标列实际约 100px；表格 `scrollWidth` 与 `clientWidth` 均为 1628px。
- 模拟 100 个“本期值 + 环比值”指标单元格，横向溢出为 0。
- 恢复后页面外壳回到 1234px、右侧边距回到 482.5px、官方表格宽度回到 2920px。

---

### B040 · 插件弹窗外壳异常撑到 Chrome 最大宽度

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | 已修复（待王小熊真实弹窗验收）— v4.9.1 |
| **提出时间** | 2026-07-31 |
| **涉及文件** | `extension/popup.html` / `tests/verify_qianfan_full_table.py` |
| **复现路径** | 千帆商品笔记页 → 点击扩展图标 |

**现状描述**：
- 插件实际内容仍是原来的窄栏，但弹窗视口被撑到接近 Chrome 允许的最大宽度，右侧出现大块白色空白。

**期望效果**：
- `html` 与 `body` 双重锁定为 360px 紧凑宽度，并裁掉横向溢出。
- 千帆宽屏样式只注入千帆标签页，不得影响 `chrome-extension://` 弹窗页面。

**验收标准**：
1. 插件弹窗宽度固定为 360px，不再出现右侧大块白色空白。
2. 普通笔记、商品页、账号主页和千帆页四类入口都不横向溢出。
3. 千帆页面进入宽屏模式时，插件弹窗尺寸保持不变。

**修复与验证记录**：
- `html`、`body` 和千帆功能卡片均增加宽度边界；弹窗内容、所有子元素最右边界和 `body.scrollWidth` 均为 360px。
- 已生成普通入口与千帆宽屏入口两张 360px 验收截图；真实 Chrome 扩展弹窗仍需安装 v4.9.1 后回读确认。

---

### B001 · 帮助页（help.html）视觉粗糙，不像正式产品

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | `extension/help.html`（整页重做，实际 95 行） |
| **复现路径** | Chrome 加载扩展 → 点扩展弹窗右上角「使用帮助」链接 → 打开 `chrome-extension://<id>/help.html` |

**现状描述**：
- 整体像未排版的 markdown 直出，无卡片、无分区背景色、无 hover/分隔视觉
- H1 22px、H2 16px，字号层级压缩太紧（H2 比正文 14px 只大 2px）
- 「三种使用方式」的 A/B/C 红色 tag 跟小红书品牌色 `#FF2442` 一致但 UI 上很突兀（背景红 + 白字 + 角标小）
- 状态徽标说明里 `✓` `+1` `!` 全用 `<code>` 包裹，颜色描述只在文字里写「绿色/红色」，**没有真的彩色展示**，用户看不出来
- "常见问题"用 H3 标题，但 H3 没有任何样式（继承默认），跟正文混在一起，层级断裂
- "打开飞书表 →" 链接是默认蓝色，无 hover、无图标
- 顶部没有产品 logo 或品牌锚定，开局就是一个标题
- 整体页面宽度 720px 居中，但没有 max-height 截断或滚动锚定，长内容一泻到底

**期望效果**：
- 顶部有产品名 + 版本号 + logo 的 hero 区
- 三大区块用卡片（白底 + 圆角 12px + 浅灰边框 `#E4E4E7`）分隔
- 「三种使用方式」用 3 列卡片网格（A/B/C 各占一格），带步骤编号 + 图标
- 「状态徽标说明」直接用真实样式渲染徽标（绿/红实际显示，不是文字描述）
- 「常见问题」用折叠卡片（`<details>`）或左侧带色条的提示框（warning/error/info 三色）
- 配色继承小红书品牌主色 `#FF2442` 但只用于强调（按钮、徽章），大面积用中性灰白
- 字号层级：H1 28px / H2 20px / H3 16px / body 14px，行高 1.7
- 链接默认 `#FF2442`，hover 加下划线 + 颜色变深

**验收标准**：
1. 在 1280×800 浏览器窗口打开 help.html，截图发给王小熊确认"看起来像个正式产品"
2. 所有徽标（✓ +1 !）用真实颜色渲染（而非文字描述）
3. 三种使用方式区块的卡片在浏览器缩放到 768px 宽度时仍能正常显示（不挤压破版）
4. 折叠/展开常见问题的交互流畅（如使用 `<details>`，原生即可）

**预估工时**：1-1.5 小时

---

### B002 · 扩展图标全部重做（4 个尺寸差异化设计）

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | `extension/icons/icon-16.png` / `icon-32.png` / `icon-48.png` / `icon-128.png`（4 个 PNG 全部替换） |
| **复现路径** | Chrome 地址栏 `chrome://extensions` → 扩展卡片左上角图标；Chrome 工具栏扩展图标 |

**现状描述**：
- 4 个尺寸都是同一张图缩放：红底圆角 + 白色"红"字
- 16x16 完全糊掉，看不出是什么
- 跟"收录小红书"功能完全无关，员工看到"红"字猜不到是什么扩展
- 跟小红书官方 logo 配色撞色（员工可能误以为是小红书出的产品）
- 视觉上像未完成的占位图，不像正式产品

**期望效果**：
- 4 个尺寸**差异化设计**（而非缩放同一张图）：
  - **128/48**：完整设计 — 主视觉 + 品牌色 + 功能暗示（如"收"字 / 笔记本 + 下载箭头 / 收藏夹图标）
  - **32/16**：极简版 — 单一标志性符号（让小尺寸不糊）
- 配色避开小红书品牌主色 `#FF2442`（避免混淆），可用对比色（如墨绿、靛蓝、低饱和度的暖红）
- 视觉上一眼联想到"采集 / 收藏 / 入库"，而非小红书本身
- 风格：扁平化 + 圆角，符合 Chrome 扩展图标规范

**解决方案（已锁定）**：**调用 AI 生图**（Midjourney / DALL-E / Claude）
- 提示词参考（开发可优化）：
  ```
  Chrome browser extension icon, flat design, rounded square 128x128,
  represents "one-click archiving social media notes to spreadsheet",
  central icon: stylized notebook with download arrow + small star/bookmark,
  color palette: deep teal or muted red, NOT pure red #FF2442,
  clean professional SaaS aesthetic,
  must be recognizable at 16x16 thumbnail,
  provide 4 size variants with simplified version for small sizes
  ```
- 流程：AI 生成 3-5 个候选 → 王小熊选 1 个 → 导出/适配 4 个尺寸 → 替换

**验收标准**：
1. 4 个 PNG 全部替换，文件名不变（manifest.json 无需改动）
2. 16x16 在 Chrome 工具栏能看清主体元素（不糊）
3. 128x128 拉大到扩展商店预览能看清细节
4. 王小熊主观确认"看起来像正式产品"
5. 找 3 个同事盲测"猜一下这是干嘛的扩展" → 答对率 ≥ 50%

**预估工时**：AI 生图 1h + 选稿 0.5h + 导出替换 0.5h = **2 小时**

---

### B003 · 全站 emoji 图标替换为 SVG icon set

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | `extension/popup.html` / `options.html` / `onboarding.html` / `help.html`（4 个 HTML 全改） |
| **复现路径** | 打开任一扩展页面 → 所有按钮 / 标题 / 状态徽标的图标位置 |

**现状描述**：
- 4 个页面共出现 **20+ 个 emoji 当图标**：📌 🚪 ⚙️ 🤔 ⚠️ ⏳ 📜 🔗 📥 ➕ 💾 🔌 🔐 🔑 📊 🎉 📱
- Mac 渲染为 Apple emoji（彩色立体），Windows 是 Segoe UI Emoji（扁平卡通），样式完全不同
- 员工大概率是 Windows，看到的效果跟王小熊（Mac）截然不同
- emoji 无 hover / active 状态，鼠标移上去无反馈
- 图钉 📌 当 logo 跟功能无关，纯凑数

**期望效果**：
- 引入 **lucide-icons**（开源 MIT，1000+ SVG 图标，体积小）
  - 官网：https://lucide.dev/
  - 直接复制 SVG 代码内嵌 HTML，无需打包工具
- 替换映射表：

| 当前 emoji | 用途 | 建议 SVG（lucide） |
|---|---|---|
| 📌 | logo（4 处） | 用 B002 设计的扩展图标缩略版 |
| ⚙️ | 设置 | `settings` |
| 🚪 | 退出登录 | `log-out` |
| 🤔 | 空状态 | `search-x` |
| ⚠️ | 错误/警告 | `alert-triangle` |
| ⏳ | 加载 | `loader-2`（带旋转动画） |
| 📜 | 最近收录 | `history` |
| 🔗 | 打开外链 | `external-link` |
| 📥 | 主按钮（收录） | `download-cloud` |
| ➕ | 新建分类 | `plus` |
| 💾 | 保存 | `save` |
| 🔌 | 测试连接 | `plug` |
| 🔐 | 登录 | `shield-check` |
| 🔑 | 激活码 | `key` |
| 📊 | 绑表 | `table-2` |
| 🎉 | 完成 | `check-circle-2`（绿色填充） |
| 📱 | 扫码 | `smartphone` |

**验收标准**：
1. 4 个 HTML 文件全部完成替换，`grep -oE '[\x{1F300}-\x{1F9FF}]'` 找不到 emoji（文案除外）
2. 在 Windows + Mac 两个系统打开扩展，所有图标视觉一致
3. 所有图标有 hover 颜色变化（继承 `--brand` 主色）
4. 主按钮的图标 + 文字垂直居中对齐

**预估工时**：3-4 小时（含找图、替换、调样式）

---

### B004 · options.html 设置页整页重做

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | `extension/options.html`（整页重做，实际 105 行） |
| **复现路径** | 扩展图标 → ⚙️ → 打开设置页 |

**现状描述**：
- 720px max-width 居中，无卡片化布局，跟 markdown 直出一样
- "连接配置"（API Endpoint + Token）和"高级设置"（重复笔记策略）混在一起，无视觉分组
- "高级设置" H2 用 inline style 写，跟全局样式断裂
- 输入框是 monospace 字体，placeholder 也是 mono 看起来很冷
- 测试连接结果只有一行 banner，缺响应时间 / 飞书表名等具体反馈
- 完全没有当前账号信息展示 → 拆分到 B005
- 按钮组排版散乱（💾 保存 / 🔌 测试连接）

**期望效果**：
- 改为**卡片化布局**，每个分组一张卡片：
  1. **账号信息卡片**（详见 B005）
  2. **连接配置卡片** — API Endpoint + Token + 测试连接按钮
  3. **采集行为卡片** — 重复笔记策略 + 未来更多偏好预留位
  4. **危险操作卡片**（可选）— 重置 / 退出登录 / 清除本地数据
- 顶部加面包屑或返回按钮（员工可能不知道怎么关掉设置页）
- 测试连接成功后展示：✓ 服务器在线 / ✓ Token 有效 / ✓ 飞书表可访问 + 响应时间
- 配色与 popup 统一（用 popup 那套 CSS variables）

**验收标准**：
1. 在 1280×800 浏览器打开，设计感跟 onboarding.html 相当或更好
2. 4 个卡片视觉清晰，有适当 padding 和阴影
3. 测试连接展示 3 个分步检查结果（不只是"成功"二字）
4. 王小熊截图确认"看起来像正式 SaaS 产品的设置页"

**预估工时**：3 小时（不含 B005，含的话 4-5 小时）

---

### B005 · 设置页新增「当前账号信息」区块（v4 OAuth 必需）

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | `extension/options.html`（新增区块） / `extension/options.js`（新增逻辑，需调 `/api/me`） |
| **复现路径** | 扩展图标 → ⚙️ → 期望看到顶部"账号信息"卡片 |

**现状描述**：
- v4.0.0 已上线 OAuth 自助登录架构，但**设置页完全没展示当前账号信息**
- 员工激活后忘了"我绑的是哪个飞书表" → 没法在设置页确认
- 员工想换表 → 没入口
- 员工想看自己的激活状态 / 用户名 / 角色 → 没入口

**期望效果**：
- 设置页顶部新增「账号信息」卡片，展示：
  - 飞书头像 + 姓名（从 `/api/me` 返回的 `name` + `avatar_url`）
  - 用户身份徽章（普通用户 / 管理员）
  - 当前绑定的飞书表（名称 + URL，可点击跳转）
  - JWT 过期倒计时（"还有 X 天过期，过期后需重新扫码"）
  - 操作按钮：「换表」「退出登录」「重新登录」
- 调用接口：`GET /api/me`（v4 已实现）
- 加载态：骨架屏 / loading spinner

**验收标准**：
1. 王小熊打开设置页，顶部看到自己的飞书头像 + 姓名 + 绑定表名
2. JWT 过期倒计时正确显示（如"还有 28 天"）
3. 点「换表」能跳到换表流程（如换表流程未实现，先标 TODO 不阻塞）
4. 退出登录后跳回 onboarding 屏 1（飞书登录）
5. legacy 用户（X-Auth-Token 路径，无 JWT）也能显示"Legacy 模式 - 王小熊"

**预估工时**：2-3 小时（依赖 `/api/me` 已实现，主要做前端 UI + 状态管理）

---

### B006 · popup 主按钮"📥 收录到飞书"替换为 SVG 图标

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | `extension/popup.html:402`（主按钮行） |
| **复现路径** | 任意小红书笔记页 → 点扩展图标 → 主按钮"📥 收录到飞书" |

**现状描述**：
- 主按钮 emoji `📥` 在 Windows 是黑白线稿，Mac 是彩色 3D，视觉差异极大
- 作为最核心 CTA，应该有最一致的视觉
- 跟整页风格不搭（页面整体偏现代扁平，emoji 突兀）

**期望效果**：
- 替换为 `lucide:download-cloud` 或 `lucide:save-all` SVG
- 图标大小 16px，跟"收录到飞书"文字垂直居中
- 图标颜色继承按钮的白色

**验收标准**：
1. Windows 截图 + Mac 截图，主按钮视觉完全一致
2. 图标 + 文字垂直居中（无错位）

**预估工时**：随 B003 一起改，单独估 15 分钟

---

### B007 · popup 仪表盘"失败"状态视觉权重升级

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | `extension/popup.html:360-367`（仪表盘 HTML） + `extension/popup.html:72`（`.stat-failed` 样式） |
| **复现路径** | 弹窗 → 仪表盘有失败数（> 0）时观察"失败"格 |

**现状描述**：
- 仪表盘 4 列：今天 / 本周 / 累计 / 失败
- 失败数 > 0 时显示，但只是把数字颜色变红，其他视觉跟正常格一模一样
- 用户容易忽略失败状态，导致失败笔记一直堆积没人管
- 失败格虽然 `cursor: pointer`，但没有视觉提示"可点击"

**期望效果**：
- 失败格背景色变浅红 `#FEF2F2`，整格有红色细边框
- 数字加粗 / 加大
- 加 hover 效果（背景变深红、cursor: pointer 强化）
- 数字旁加 ⚠️ 图标（B003 用 SVG 替代）
- 数字 > 0 时整格加微微 pulse 动画（吸引注意）

**验收标准**：
1. 失败数 > 0 时一眼能看到，用户不会忽略
2. 鼠标移上去明显能感觉"可点击"
3. 失败数 = 0 时整格隐藏（保留现有逻辑）

**预估工时**：1 小时

---

### B008 · popup 空状态加插画

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | `extension/popup.html:351-355`（非小红书页空状态）+ 仪表盘空数据状态 |
| **复现路径** | 1）非小红书页打开弹窗 2）新员工累计收录 0 条时打开弹窗 |

**现状描述**：
- 非小红书页只显示"🤔 当前不是小红书笔记页"一行文字 + emoji
- 已收录 0 条时仪表盘只显示"-"，非常冷清
- 「最近收录」展开后空状态也是"加载中…"或空白

**期望效果**：
- 加 SVG 空状态插画（轻量风格，参考 undraw / humaaans）
- 新员工"还没收录过笔记"加引导提示："开始收录第一条笔记 →" + 教学按钮
- 非小红书页面引导："去小红书首页 →"（带跳转链接）

**验收标准**：
1. 新员工第一次打开弹窗能看到欢迎引导（不是冰冷的 `-`）
2. 非小红书页有插画 + 跳转链接

**预估工时**：1.5 小时

---

### B009 · onboarding 屏 1.5「等待飞书授权」体验优化

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23（CLAUDE.md 已记录为遗留 bug） |
| **涉及文件** | `extension/onboarding.html:211-226`（屏 1.5 HTML） + `extension/onboarding.js`（启动逻辑） |
| **复现路径** | 新用户走 onboarding → 扫码登录后 |

**现状描述**（CLAUDE.md 已记录）：
- 扫码授权后如果 background reload 太慢，用户会看到「⏳ 等待飞书完成授权」短暂闪屏 → 然后跳激活屏
- 体验不平滑

**期望效果**（CLAUDE.md 已建议）：
- onboarding 启动时检测 `chrome.storage.local.jwt` 已存在 → 跳过等待屏，直接进激活屏
- 等待屏改为加载骨架屏 + spinner（而非"等待飞书完成授权"的不确定文案）
- 15 秒兜底逻辑保留（万一 background 没收到 token）

**验收标准**：
1. 王小熊重新走一次 onboarding（清 `chrome.storage` 后），扫码 → 直接到激活屏，无闪屏
2. 模拟 background 监听失败（断网后扫码），15 秒后兜底手动粘贴框显示

**预估工时**：1.5 小时

---

### B010 · 全站建立统一设计系统

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | 新增 `extension/styles/design-system.css` + 4 个 HTML 引入 |
| **复现路径** | 对比 popup.html / options.html / help.html 三个页面的样式定义 |

**现状描述**：
- **3 套样式系统并存**：
  - popup.html：完整 CSS variables（`--brand`, `--gray-50` ~ `--gray-900`）
  - options.html：写死十六进制色（`#FF2442` `#E4E4E7` `#71717A`）
  - help.html：写死色 + 自定义 `.tag` 样式
- 修改主题色需要改 3 处
- 同一种灰色在不同页面可能值不一样（已确认 popup 的 `--gray-500: #71717A` 跟 options/help 写死的 `#71717A` 重复定义）
- 没有 spacing scale，各页 padding/margin 手动调

**期望效果**：
- 提取共享 `design-system.css`，包含：
  - 颜色变量（brand + gray scale + 状态色 green/red/orange）
  - 字体大小 scale（`--text-xs/sm/base/lg/xl`）
  - spacing scale（`--space-1/2/3/4...`）
  - 圆角、阴影、过渡时间统一
  - 通用组件类（`.card`, `.btn`, `.input`, `.badge`）
- 4 个 HTML 都 `<link rel="stylesheet" href="styles/design-system.css">`
- 各页面 `<style>` 里只写自己独有的布局

**验收标准**：
1. `grep -E "#[0-9A-Fa-f]{6}" extension/*.html` 几乎找不到硬编码颜色
2. 改 `--brand` 一次，4 个页面全变
3. 同一种灰色在 3 个页面值完全一致

**预估工时**：3-4 小时

---

### B011 · 引入 SVG icon system（实施层，配合 B003）

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | 新增 `extension/icons/svg-sprite.svg`（雪碧图）+ 各 HTML 引用 |
| **复现路径** | 配合 B003 一起做 |

**现状描述**：
- B003 描述了 emoji 问题与替换映射
- 实施方式有 2 种：
  - A：每个图标内嵌完整 SVG → HTML 体积大、难维护
  - B：用 SVG sprite（雪碧图） + `<use>` 引用 → 体积小、易维护

**期望效果**：
- 用方案 B：建 `icons/svg-sprite.svg`，内含所有用到的图标 `<symbol id="xxx">`
- HTML 使用方式：`<svg class="icon"><use xlink:href="icons/svg-sprite.svg#download-cloud"/></svg>`
- 统一 `.icon` 样式（默认 16px，可通过 class 调大小）
- 颜色继承 `currentColor`（跟随上下文文字颜色变化）

**验收标准**：
1. 所有图标定义集中在一个 sprite 文件
2. 新增图标只需在 sprite 加一个 `<symbol>` + HTML 加一行 `<use>`
3. hover 时图标颜色随父元素文字颜色变化

**预估工时**：2 小时（配合 B003 实施，可合并）

---

### B012 · onboarding 步骤切换加 fade 过渡动画

| 字段 | 内容 |
|---|---|
| **优先级** | P3 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | `extension/onboarding.html:73-75`（`.screen` 样式） |
| **复现路径** | 走 onboarding，观察从屏 1 → 屏 2 → 屏 3 → 屏 4 的切换 |

**现状描述**：
- 屏切换是硬切（`display: none` → `display: block`），没有过渡
- 体验生硬

**期望效果**：
- 屏切换加 fade in / slide up 动画（200-300ms）
- 步骤指示器（1.飞书登录/2.激活码/3.绑表）有色块流动动画

**验收标准**：
1. 切换流畅，无闪烁
2. 用户能清楚感知"从第 X 步走到了第 X+1 步"

**预估工时**：1 小时

---

### B013 · onboarding 完成屏加 confetti 庆祝动画

| 字段 | 内容 |
|---|---|
| **优先级** | P3 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | `extension/onboarding.html:266-279`（屏 4 完成屏） |
| **复现路径** | 走完 onboarding 4 屏 |

**现状描述**：
- 完成屏只显示「🎉 配置完成！」+ 文字提示
- 缺成就感 — 员工花 5-10 分钟走完 onboarding，应该有奖励反馈

**期望效果**：
- 进入完成屏触发 confetti（彩纸雨）动画
- 推荐库：[canvas-confetti](https://github.com/catdad/canvas-confetti)（10KB，纯 JS 无依赖）
- 动画时长 2-3 秒，自动消失
- 配合一句鼓励文案："欢迎加入团队 [用户名]！开始收录你的第一条笔记吧 →"

**验收标准**：
1. 走完 onboarding 触发 confetti
2. 动画结束后不挡 UI（点关闭按钮无遮挡）

**预估工时**：30 分钟

---

### B014 · 版本号管理体系（bump-version.sh + manifest bump + CHANGELOG）

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | 已修复（待验收）— 本批次完成 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | 新增 `bump-version.sh` / 改 `extension/manifest.json` / 新增 `CHANGELOG.md` |
| **复现路径** | 查看 `extension/manifest.json` 的 version 字段、`dist/` 目录 zip 列表 |

**现状描述**：
- 源码改动后 manifest.json 一直 4.0.0 没 bump
- dist 目录无 zip（CLAUDE.md 之前虚假记录"v4.0.0.zip"实际不存在）
- 发版无版本对账机制，团队不知道哪个 zip 对应哪个版本

**期望效果**：
- bump-version.sh 一键完成 bump + 写 CHANGELOG + 打 zip
- 三处版本号（manifest / CHANGELOG / zip 文件名）始终一致
- 支持 patch / minor / major 三种递增方式

**验收标准**：
1. 跑 `./bump-version.sh patch` 后 manifest+CHANGELOG+zip 三者版本号一致
2. zip 排除 `secrets.js` 和 `secrets.js.example`
3. CHANGELOG 新条目插在文件顶部，旧条目保留

**预估工时**：已完成

---

### B015 · 更新日志面板（铃铛 + 弹窗）

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | 已修复（待验收）— 本批次完成 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | 新增 `server/changelog seed` / `popup.html` / `popup.js` / 新增 `extension/changelog-modal.html`（如果分文件） |
| **复现路径** | 升级到 v4.1.0 后第一次打开 popup |

**现状描述**：
- v4 上线后没有更新日志机制，员工不知道每次更新动了什么
- 团队成员升级后毫无感知，新功能上线没人知道

**期望效果**：
- popup 顶部加铃铛，新 minor/major 版本自动弹窗显示更新内容
- 弹窗内容来自服务端 `/api/changelog`（admin 通过 `/api/admin/changelog` 维护）
- 已读后铃铛红点消失，下次升级再出现

**验收标准**：
1. 王小熊安装 v4.1.0 后第一次打开 popup 自动弹窗，展示「v4.1.0 新功能」
2. 关闭弹窗后铃铛红点消失
3. 升级到下一个 minor 版本再次自动弹窗

**预估工时**：已完成

---

### B017 · onboarding 绑表时 init_sheets 失败未汇报，前端假装配置成功（误导用户）

| 字段 | 内容 |
|---|---|
| **优先级** | **P0** |
| **状态** | ✅ 已修复（待验收）— v4.2.0 完成 |
| **提出时间** | 2026-05-23 |
| **影响范围** | 所有新用户（王小熊 admin 已踩坑，3 同事 100% 会再踩） |
| **涉及文件** | `extension/onboarding.html`（屏 3 绑表 + 屏 4 完成屏） / `extension/onboarding.js`（bind-sheet 处理） / `server/app.py:956`（auto-create 7 categories 循环） |
| **复现路径** | 1）新建飞书表 2）只把 bot 加为协作者，但**没在表格设置里开「允许应用访问/添加文档应用」** 3）走 onboarding 屏 3 贴表 URL → 点「绑定 + 自动建分类」 4）看到「🎉 配置完成」5）回扩展点收录 → 报 91403 Forbidden |

**现状描述**（已用真实数据验证）：
- 王小熊 onboarding 时按指引"添加 bot 为可编辑协作者"，看到「🎉 配置完成」
- **真相**：绑表时调用的 `init_sheets`（自动建 7 个分类 sheet）**全部 91403 失败**，但前端没拦截
- 复现：服务器 Python 直接复测，`w.get_sheets_info()` 返回 OK（读权限），`w.create_sheet()` 抛 LarkAPIError 91403（写权限缺失）
- 王小熊飞书表实际只有 1 个 `Sheet1`，缺：起号/爆款/同行精选/潜力款/标题公式/互动引导/待研究 共 7 个
- 用户以为配置完成，几小时后才发现根本不能用，且不知道为什么
- **根因**：飞书新版权限模型 — 仅"协作者-可编辑"不够，还要单独在表格「设置 → 允许应用访问 / 添加文档应用」里授权

**期望效果**：
1. **绑表时严格校验**：调 `init_sheets` 后逐个 sheet 检查是否真建成功，任一失败立刻报错 + 屏 3 不放行
2. **错误文案精准**：不显示"飞书 API 91403 Forbidden"，改为：
   > ❌ **bot 没有写权限**
   > 你已经把 bot 加为协作者，但飞书新版还需要单独授权"应用访问"。
   > 操作：飞书表 → 右上角 ⋯ → **设置** → **允许应用访问** → 添加「小红书收录助手」
   > [▶ 看图教程]（截图链接）
3. **onboarding 屏 3 加预检按钮**：「检测权限」按钮，未通过不让走下一步
4. **onboarding 文案修订**：屏 3 的"⚠️ 先在飞书做这两件事"改为 3 件事，加上"开启应用访问权限"
5. **完成屏加最终自检**：屏 4 前调一次 `/api/me` + `/api/sheets`，确认能读到 7 个分类 sheet 才允许进"配置完成"屏

**验收标准**：
1. **负向用例**：新建一个表，只加 bot 为可阅读 / 加协作者但不开应用访问 → 走 onboarding → 必须在屏 3 被拦截，且错误文案能让用户 30 秒内自己修
2. **正向用例**：按正确步骤（协作者 + 应用访问），onboarding 走完后飞书表里真的有 7 个分类 sheet
3. **回归**：王小熊（已踩坑用户）重新走一次 onboarding，能补建出缺失的 6 个分类

**预估工时**：4-5 小时（含截图教程 1h、文案 0.5h、屏 3 预检 1.5h、init_sheets 错误处理 1.5h、文档教程更新 0.5h）

---

### B018 · 缺「权限自检」机制 — 所有写操作前应主动验证 bot 权限

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | ✅ 已修复（待验收）— v4.2.0 完成（`/api/permissions/check` 已上线，options 页面自检按钮待加） |
| **提出时间** | 2026-05-23（B017 关联） |
| **涉及文件** | 新增 `server/lark_writer.py:check_write_permission()` 方法 / 新增 `GET /api/permissions/check` 路由 / `extension/options.html` 新增「权限自检」按钮 |
| **复现路径** | 任何对飞书表的写操作（新建分类、收录笔记、删除分类）失败时，用户只看到「91403 Forbidden」，不知道如何排查 |

**现状描述**：
- B014 是 onboarding 时机的特例，但运行期任何时候用户都可能在飞书后台收回 bot 权限 → 扩展无法预知
- 当前任何 91403 都只展示原始飞书错误码，对非技术用户毫无用处
- 没有主动诊断工具

**期望效果**：
- **后端新增**：`POST /api/permissions/check`
  - 内部逻辑：用当前用户的 spreadsheet_token，尝试 create 一个临时 sheet 后立刻 delete
  - 返回：`{ read: true/false, write: true/false, fail_reason: "..." }`
- **前端集成**：
  - options 设置页"账号信息"卡片下加「检测权限」按钮
  - 任何写操作返回 91403 时，自动调一次 `/api/permissions/check`，给出精确诊断 + 修复指引
- **错误文案分类**：
  - 91403 + write false + read true → "bot 没写权限，请去飞书表设置开启应用访问"
  - 91403 + write false + read false → "bot 被移出协作者，请重新加为可编辑"
  - 其他 → "未知权限错误，联系王小熊"

**验收标准**：
1. 在 options 页点「检测权限」 → 3 秒内返回结果 + 明确指引
2. 模拟 bot 被移出协作者 → 收录时自动诊断 + 引导用户修复
3. 模拟"协作者但无应用访问" → 同上

**预估工时**：3-4 小时

---

### B019 · 飞书表格美化（对外展示场景）

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | ✅ 已修复（待验收）— v4.2.0 完成 |
| **提出时间** | 2026-05-23 |
| **涉及文件** | `server/lark_writer.py:setup_sheet_template()` 重写 + 新增 `server/backfill_sheet_style.py`（一次性脚本） |
| **复现路径** | 王小熊飞书表的表头/数据行视觉粗糙，不适合对外展示给同行 |

**现状描述**：
- 表头无加粗、无背景色、无居中
- 数据行无斑马纹、无边框
- 列宽未配置，长文本被压成小条
- 没有冻结，往下滚动看不到表头

**期望效果**：
- 表头：加粗 + 浅灰背景 `#F3F4F6` + 居中 + 细边框 + 13px + 行高 32px
- 数据行：斑马纹（偶数 `#F9FAFB`）+ 细边框 + 12px
- 双冻结：首行 + 首列
- 列宽：A60/B240/C100/D280/E220/F400/G-J70/K180/L130/M100/N220
- 跑 backfill 脚本 update 王小熊现有表

**验收标准**：
1. 王小熊打开飞书表看到美化后效果
2. 新员工 onboarding 后建出来的 sheet 自动应用同款样式
3. 滚动到第 100 行表头仍可见（冻结生效）

**预估工时**：1.5 小时 ✅ 已完成

---

### B020 · OAuth HTTPS 升级 + 域名（推迟）

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | 🚧 已立项，推迟（需用户配置域名 + Let's Encrypt） |
| **提出时间** | 2026-05-23 |
| **涉及文件** | `server/nginx-xhs-collect.conf` / `extension/background.js` / `extension/manifest.json` host_permissions |
| **复现路径** | 当前 OAuth JWT 通过 `http://14.22.112.147:8866/auth/done?token=xxx` 回传 → token 进 Referer / 浏览器历史 / 中间代理 |

**现状描述**：
- v4.2.0 已加 OAuth state nonce 防 CSRF（代码层 hardening）
- 但仍是 HTTP 明文，token 暴露在 URL query
- 真实风险：内网 + 内部 3 同事 → 攻击面小
- 但长期不应该明文 HTTPS 传 token

**期望效果**：
- 注册域名（如 xhs-collect.wangxiaoxiong.com）
- Let's Encrypt 拿 SSL 证书
- nginx 改 listen 443 ssl
- 扩展 manifest host_permissions 改 https://
- 后端 OAuth 回调用 https URL

**验收标准**：
1. 浏览器地址栏看到 🔒 HTTPS
2. token 在 URL 但已加密传输
3. Mixed content 报错为 0

**预估工时**：3-4 小时（含域名注册 + DNS + Let's Encrypt + 部署 + 扩展更新）

**推迟原因**：王小熊需要先决定用哪个域名 + 走 DNS 解析（这一步 Claude 做不了）

---

### B021 · legacy 用户 `/api/me` 返回 active 但 spreadsheet_token 可能为空

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23（CLAUDE.md 清理时从旧遗留 bug 区迁出） |
| **影响范围** | legacy X-Auth-Token 路径的老用户（v3.1 时期遗留），主要是王小熊 admin 本人 |
| **涉及文件** | `server/app.py` 的 `/api/me` 路由 + 前端 `extension/popup.js` 的状态判断 |
| **复现路径** | 1）用 legacy X-Auth-Token 登录（不走 OAuth）2）调 `/api/me` 3）返回 `status: "active"` 但 `spreadsheet_token` 可能为空 4）前端显示"已就绪" 5）实际收录时 403 |

**现状描述**：
- legacy 用户的 status 默认是 active（因为没走 OAuth pending 流程）
- 但 spreadsheet_token 可能从未绑过
- 前端没有处理这种"active 但无绑表"的边界
- v4.2.0 改造了 OAuth 流程严格化（B017），但 legacy 路径未明确修

**期望效果**：
- 方案 A：让 legacy 用户也走 OAuth 流程（更彻底，但有迁移成本）
- 方案 B：前端在 status=active 时也检查 spreadsheet_token，未绑则提示去绑

**验收标准**：
1. 王小熊本人验证 legacy 路径无此问题（或确认已走 OAuth）
2. 模拟 legacy 用户未绑表 → 收录时前端应提示"请先绑表"而不是 403

**预估工时**：1-2 小时

---

### B022 · dashboard 第一次冷启动 4 秒（缓存未命中慢）

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **状态** | 待修复 |
| **提出时间** | 2026-05-23（CLAUDE.md 清理时从旧遗留 bug 区迁出） |
| **影响范围** | 任何用户第一次打开 popup 看 dashboard 时 |
| **涉及文件** | `server/app.py` 的 dashboard 数据 endpoint + `extension/popup.js` 的 dashboard 加载逻辑 |
| **复现路径** | 1）清 chrome.storage 的 dashboard 缓存 2）打开 popup 3）等 4 秒才看到今天/本周/累计数据 |

**现状描述**：
- 缓存命中后 70ms 响应（快）
- 但第一次冷启动需要 4 秒（飞书 API 查表 + 聚合慢）
- 用户体验：打开 popup → 看到一片空白 → 4 秒后突然出数据

**期望效果**：
- 方案 A：缓存预热（后台定时刷新数据）
- 方案 B：后台异步（先返回缓存或骨架屏，4 秒后再 update）
- 方案 C：服务端缓存（Redis / SQLite 缓存 dashboard 数据，按用户 + 分钟级 TTL）

**验收标准**：
1. 冷启动响应时间 < 1 秒
2. 骨架屏/loading 动画过渡自然

**预估工时**：2-3 小时

---

### B023 · popup label 错字「收到」→「收录到」

| 字段 | 内容 |
|---|---|
| **优先级** | P0 |
| **状态** | ✅ 已修复（待验收）— v4.3.0 |
| **涉及文件** | `extension/popup.html:643` |
| **现状描述** | 「收到哪个分类（sheet）？」漏字，影响品牌专业感 |
| **期望效果** | 改成「收录到哪个分类（sheet）？」 |
| **验收标准** | 打开 popup → 看到完整文案 |

---

### B024 · onboarding 屏 3 教学顺序反（未先教新建飞书表）

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | ✅ 已修复（待验收）— v4.3.0 |
| **涉及文件** | `extension/onboarding.html` 屏 3 顶部 |
| **现状描述** | 屏 3 直接要求"加文档应用"，但用户可能还没新建飞书表 |
| **期望效果** | 屏 3 顶部加"📝 还没有飞书表？先新建一个"前置 tip，含飞书云空间链接 |
| **验收标准** | 新员工走 onboarding 时屏 3 能看到前置步骤 |

---

### B025 · onboarding 屏 1.5「新企业」措辞不清

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | ✅ 已修复（待验收）— v4.3.0 |
| **涉及文件** | `extension/onboarding.html:215` |
| **现状描述** | 「请选择新企业身份」对员工无意义，不知道是哪个 |
| **期望效果** | 改成「请选择「武汉眸佑商贸有限公司」企业身份」 |
| **验收标准** | 屏 1.5 显示具体企业名 |

---

### B026 · onboarding 屏 3 URL placeholder 改具体域名

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | ✅ 已修复（待验收）— v4.3.0 |
| **涉及文件** | `extension/onboarding.html:261` |
| **现状描述** | placeholder 用 `my.feishu.cn`，员工实际域名不同 |
| **期望效果** | 改成 `https://acnl9ctl8qlc.feishu.cn/sheets/xxxxxxxx` + hint |
| **验收标准** | 屏 3 placeholder 显示真实域名 |

---

### B027 · onboarding 屏 4 完成后加「打开示例笔记演练」按钮

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | ✅ 已修复（待验收）— v4.3.0 |
| **涉及文件** | `extension/onboarding.html:286` + `onboarding.js` |
| **现状描述** | 屏 4 完成后用户没引导第一次试用 |
| **期望效果** | 加「🎬 打开示例笔记演练一次」按钮，跳 `xiaohongshu.com/explore` |
| **验收标准** | 屏 4 多了演练按钮 |

---

### B028 · popup「+」加号按钮太小

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | ✅ 已修复（待验收）— v4.3.0 |
| **涉及文件** | `extension/popup.html:648` |
| **现状描述** | btn-mini class 太小，新员工注意不到能新建分类 |
| **期望效果** | 改为 88×36px + 文字「+ 新分类」+ 品牌色边框 + hover 红底 |
| **验收标准** | popup 加号按钮明显可见 |

---

### B029 · 重复笔记提示缺 next action

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | ✅ 已修复（待验收）— v4.3.0 |
| **涉及文件** | `extension/popup.html:632-641` + `popup.js` |
| **现状描述** | 「⚠️ 这条已收录过」只说事实不告诉用户怎么办 |
| **期望效果** | 加 2 个按钮：「✏️ 在飞书表里改这行」(打开飞书表) + 「跳过」(隐藏提示) |
| **验收标准** | 重复时能看到 2 个按钮，点了有响应 |

---

### B030 · popup 关闭后无 Chrome 通知兜底

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | ✅ 已修复（待验收）— v4.3.0 |
| **涉及文件** | `extension/manifest.json` (加 notifications 权限) + `background.js` + `popup.js` (handleCollect) |
| **现状描述** | popup 失焦自动关，API 完成无反馈 |
| **期望效果** | 用 `chrome.notifications.create` 在 API 完成后弹原生通知，点通知打开飞书表 |
| **验收标准** | popup 收录后立即关闭 popup → 数秒后弹通知 |

---

### B031 · popup 最近收录默认折叠（高频功能藏起来）

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **状态** | ✅ 已修复（待验收）— v4.3.0 |
| **涉及文件** | `extension/popup.html:660-666` |
| **现状描述** | 最近收录是高频功能但默认折叠 |
| **期望效果** | 默认展开（已 eager load） |
| **验收标准** | popup 打开就能看到最近收录列表 |

---

### B032 · 没有快捷键 Cmd+Shift+X 一键收录

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **状态** | ✅ 已修复（待验收）— v4.3.0 |
| **涉及文件** | `extension/manifest.json` (commands) + `background.js` (chrome.commands.onCommand) |
| **现状描述** | 每次必须点扩展图标 → 等 popup → 选分类 → 收录，高频用户痛 |
| **期望效果** | `Cmd+Shift+X` 触发：用上次选的分类 + 备注空 + 后台收录 + chrome.notifications 反馈 |
| **验收标准** | 小红书页按 Cmd+Shift+X → 弹「正在收录…」→ 数秒后弹「✅ 收录成功」 |

---

### B033 · popup 首次加载全是"加载中…"缺 skeleton

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **状态** | ✅ 已修复（待验收）— v4.3.0 |
| **涉及文件** | `extension/popup.html:576-595` (CSS skeleton) + 626-629 (page-title/url) + 669-678 (recent-list) |
| **现状描述** | popup 第一次打开冷的"加载中…"文字，看起来很乱 |
| **期望效果** | 加 `.skeleton` shimmer CSS 动画，page-title / url / recent-list 占位优雅 |
| **验收标准** | 首次打开 popup 看到流光骨架，不是冷文字 |

---

### B034 · 飞书表长文案撑高行高（B019 延伸）

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | ✅ 已修复（待验收）— 本批次 |
| **提出时间** | 2026-05-23 |
| **影响范围** | 所有用户的飞书表 |
| **涉及文件** | `server/lark_writer.py:setup_sheet_template` + `backfill_sheet_style.py` |
| **复现路径** | 收录长文案笔记（如 10+ 行） → 飞书表整行行高被撑到 20+ 行 → 视觉不一致 |

**期望**：所有数据行固定 32px 行高 + 文案列不换行（双击查看全文）

**实际修复方式**：
- 飞书 style v2 API **没有 wrap/wrapStrategy 字段**（已查官方文档确认）
- 唯一可用手段 = `dimension_range` 强制 fixedSize 行高 32px
- 飞书 API 90204 坑：批量更新行高时必须用 `length` 字段，不能用 `endIndex`

**v2 修订**（2026-05-23 王小熊选 A 方案）：
- v1 的"保护已有数据行"策略**没解决用户痛点**（截图里第 6 行就是已有数据行的长文案）
- v2 改为**全表数据行（含旧封面图行）一律 32px**
- 接受 trade-off：旧封面图被压成 32px 缩略（双击图片可看大图，飞书标准行为）
- 分段批量（每 100 行一批）避免飞书 API 限制

**v3 闭环修订**（2026-05-23 Codex 审计抓到）：
- **v2 漏修关键链路**：`write_record` 在新写入记录后强制 `set_row_height(..., 260)`，**会立刻覆盖 backfill 的 32px**
- 后果：v2 backfill 完看上去 OK，但**任何新收录笔记**写入后行高又被改回 260px → 用户痛点回归
- v3 修复：删掉 `lark_writer.py:689-692` 的 `set_row_height(260)` 调用
- 新行直接继承模板预设的 32px（setup_sheet_template + backfill 已批量设）

**v5 分隔符调优**（2026-05-23 用户反馈）：
- v4 用 ` · ` 中点用户实测"太密影响阅读"
- v5 改单空格：飞书表 F 列像 D 列标题一样横向连续阅读，更自然
- 工具：`rerun_flatten_from_backup.py` 从 desc_backup 表原文重做（不基于已替换内容二次替换）
- 王小熊 9/9 行已用新分隔符重写

---

**v4 终极修复**（2026-05-23 用户截图证明 v3 失效）：
- **根因发现**：飞书 `fixedSize 32px` 对超长 cellValue **silent fail**！截图里第 12 行设了 32px 但仍被撑高
- **真因**：cellValue 含 `\n` 换行符（F12 含 48 个 \n），飞书自动渲染成多行，绕过 fixedSize
- **方案**：写入前 `\n` → ` · ` 替换，源头消除换行符，飞书自然单行
- **零数据丢失**：新建 `desc_backup` 表，原文按 row_idx 精确备份；还原走 SQL 查表，不靠字符串反向 replace
- **冲突预扫**：Pre-scan 王小熊全表，` · ` 冲突 0%（安全）
- **覆盖 update 路径**：`server/lark_writer.py` `flatten_desc()` 工具函数 + `build_row` + `app.py:154-158` update 分支都调
- **backfill 已跑**：王小熊 9/9 行替换 + 备份成功（含截图里的烂耳警告 614 字）

**验收**：
1. 王小熊重跑 backfill 后打开飞书表，所有数据行行高一致 32px ✅
2. **新收录一条长文案笔记** → 写入后该行仍是 32px（不再被 write_record 覆盖回 260px）

**已知局限**：
- 飞书 fixedSize 是否能 100% 压住超长文案需王小熊真实数据验证
- 如压不住，后续可考虑在 `build_row` 里把 desc 截断到固定长度（如 200 字）

---

### B035 · popup dup-hint 行号文字点不动（用户期望点击跳飞书表）

| 字段 | 内容 |
|---|---|
| **优先级** | P2 |
| **状态** | ✅ 已修复（待验收）— v4.3.0 |
| **提出时间** | 2026-05-23 |
| **影响范围** | 所有用户重复收录时 |
| **涉及文件** | `extension/popup.html:653` + `popup.js` 末尾 |
| **复现路径** | 收录已存在的笔记 → 看到「已在「起号图文」第 6 行」→ 用户期望点击行号跳飞书表，但 div 无 click 事件 |

**期望**：dup-detail 整段可点击，点击 = 调 openBoundSheetFresh 打开飞书表

**修复**：
- `popup.html` dup-detail div 加 `cursor:pointer` + 点状下划线视觉暗示
- `popup.js` DOMContentLoaded 区加 dup-detail click listener

**验收**：收录重复笔记 → 鼠标放到"已在「起号图文」第 6 行"上变手型 → 点击打开飞书表

---

### B036 · onboarding 屏 3 URL 校验拒绝飞书 Wiki 路径

| 字段 | 内容 |
|---|---|
| **优先级** | **P0** |
| **状态** | ✅ 已修复（待验收）— v4.3.5 |
| **提出时间** | 2026-05-23 |
| **影响范围** | 所有把表格放在飞书 Wiki 知识库里的用户（截图证实：王小熊朋友 100% 卡住） |
| **涉及文件** | `extension/onboarding.js`（前端 URL 校验） + `server/lark_writer.py:resolve_wiki_to_sheet_token` + `server/app.py:resolve_to_spreadsheet_token` |
| **复现路径** | 1）飞书 Wiki 里建电子表格 2）复制顶部 URL（`feishu.cn/wiki/{token}`）3）粘贴 onboarding 屏 3 → 前端报「URL 看着不像飞书表」 |

**根因**：前端 `parseSpreadsheetToken()` 只匹配 `/sheets/`，不识别 `/wiki/` 路径。后端 `/auth/bind-sheet` 同样问题。

**修复**：
- 前端 `parseSpreadsheetToken()` 同时识别 `/sheets/{token}` 和 `/wiki/{wiki_token}`
- 后端新增 `resolve_wiki_to_sheet_token(wiki_token, writer)`：调飞书 `wiki/v2/spaces/get_node` 拿 obj_token + 校验 obj_type=sheet
- `app.py` 新增 `resolve_to_spreadsheet_token(url_or_token)` smart resolver
- `/api/permissions/check` 和 `/auth/bind-sheet` 都用 smart resolver
- 错误友好：bot 不能访问 wiki → 返回明确的「请把 bot 加为 wiki 协作者」提示

**真实数据验证**：朋友的 Wiki URL `LAPiwUgATic7gtkujJoctaJPnoc` → 后端成功解析为 `USWAsgtpOhAi1kttNZLcJQKfncb` + bot 能读到 1 个 sheet

**已知前置**：bot「小红书收录助手」必须有该 Wiki 节点的访问权限（朋友需在 Wiki 设置里把 bot 加为协作者，跟普通表格的「添加文档应用」机制类似但路径不同）

---

### B037 · hashtag 跟文案混在 F 列，影响阅读

| 字段 | 内容 |
|---|---|
| **优先级** | P1 |
| **状态** | ✅ 已修复（待验收）— v4.3.6 |
| **提出时间** | 2026-05-23（用户反馈） |
| **影响范围** | 所有用户飞书表 F 列展示 |
| **涉及文件** | `server/lark_writer.py:extract_hashtags + build_row + setup_sheet_template` + `server/app.py` update + 新增 `server/backfill_extract_hashtags.py` |
| **复现路径** | 飞书表 F 列文案末尾有大量 `#养耳洞[话题]# #耳钉推荐[话题]#` 等，跟正文混在一起视觉乱 |

**修复**：
- 飞书表新增 **O 列「话题标签」**（300px 宽）
- 新增 `extract_hashtags(text)` 工具：正则 `#[^#\n]+?#` 提取所有 hashtag → 返回 `(clean_text, tags_str)`
- 三路径覆盖：`build_row`（新收录） + `app.py` update 分支（重复笔记更新） + `backfill_extract_hashtags.py`（一次性历史回填）
- `LAST_COL = "O"`，斑马纹/边框/列宽都跟进
- 数据源：飞书 F 列当前内容（**不依赖** desc_backup，Codex 第 3 轮抓到 desc_backup 不全）

**验证**：王小熊 15 行数据 → 13 行有 hashtag 全部成功拆分到 O 列，F 列纯净。例 O12 = `#避坑指南[话题]# #珠宝鉴定[话题]# #珠宝知识[话题]# ...`

---

## 已修复清单

### B038 · 爆款笔记采集失败 `ValueError: invalid literal for int() with base 10: '1.1万'`

| 字段 | 内容 |
|---|---|
| **优先级** | P0（爆款笔记 100% 触发，是产品主要用例） |
| **状态** | ✅ 已部署，待 e2e 验收 |
| **修复版本** | 服务器 hotfix（未 bump 扩展版本） |
| **提出时间** | 2026-05-25 |
| **修复时间** | 2026-05-25 |
| **涉及文件** | `server/collector.py:94-134`（新增 `_parse_count` 函数）+ `server/collector.py:252-255`（4 处 `_parse_count` 调用替换原 int()） |

**根因**：
- 小红书前端会把大数字格式化为人类可读字符串（"1.1万" / "10w" / "999+"）
- `collector.py:209-212` 直接 `int(interact.get("likedCount") or 0)` → ValueError → 整条采集失败
- 爆款笔记互动数大，必然踩；普通笔记互动数小（原始数字字符串）不踩 → 解释了"为什么只有爆款表格出现"

**修复方案**（已部署）：
- 新增 `_parse_count(v)` 函数，分情况处理：
  - 精确解析为 int：`1234` / `"1234"` / `"1.1万"` / `"10w"` / `"1.5亿"`
  - 原字符串入库（用户决策）：`"1万+"` / `"999+"` / `"1.5w+"` 保留外观
  - 无法解析：返 0 + stderr 警告日志（追溯用）
- 单测 15 个 case 全部通过
- scp + pm2 restart xhs-collect-api（只动目标进程，遵守安全规则）

**验收标准（待王小熊执行）**：
1. ✅ 服务器端 `_parse_count("1.1万")` 返回 11000（已确认）
2. ⏳ 浏览器打开**之前采集失败的那条爆款笔记** → popup → 收录 → 飞书表应出现新行
3. ⏳ 飞书表看「点赞 / 收藏 / 评论 / 分享」列：精确数字是 int（11000），模糊数字保留字符串（"1万+"）
4. ⏳ 服务器 pm2 logs 不应再出现 `ValueError: invalid literal for int()`

**关联观察（待后续单独录 bug）**：
- xhs-collect-api 进程重启次数从 v4.1.0 部署后涨到 38 次，平均每天崩多次。需独立排查 PM2 稳定性。

---

### B016 · popup「+」加号无响应（MV3 禁用 window.prompt）

| 字段 | 内容 |
|---|---|
| **优先级** | P0（影响核心功能：新员工无法新建分类） |
| **状态** | ✅ 已关闭 |
| **修复版本** | v4.1.0 |
| **验收人/时间** | 王小熊 / 2026-05-23 |
| **涉及文件** | `extension/popup.js:370` |

**修复方式**：popup.js 用自定义模态框替代 MV3 禁用的 `window.prompt`

**验收记录**：
- 王小熊用 v4.1.0 测试：点加号 → 弹出输入框 ✅ → 输入名字 → 调 `POST /api/categories` ✅
- 注：本次测试虽然最终建分类失败（飞书返回 91403），但失败原因是飞书表权限问题（追踪在 B017），与 B016 修复的"加号无响应"无关，B016 修复**确认有效**

---

## 候选优化项（P2，未排期）

（暂无）

---

## 改 bug 流程约定

1. **开始改前**：把状态从「待修复」改为「进行中」，写上「负责人 + 开工时间」
2. **改完后**：状态改为「已修复（待验收）」，写上「修复版本号 + 修改文件清单 + 简要改动说明」
3. **王小熊验收通过后**：状态改为「已关闭」，移动到「已修复清单」
4. **如有疑问**：在 bug 条目下加 `**疑问**：xxx`，王小熊回复后清理

---

## Changelog

| 日期 | 操作 | 负责人 |
|---|---|---|
| 2026-05-23 | 初版创建，录入 B001（帮助页 UI） | Claude |
| 2026-05-23 | 整体 UI 审查，批量录入 B002-B013（12 条 UI bug） | Claude |
| 2026-05-23 | 另一批次（v4.1 开发）录入 B014（版本号管理）+ B015（更新日志面板）+ B016（加号 bug，已修复待验收） | 其他批次 |
| 2026-05-23 | 王小熊 v4.1 复现「新建失败 91403」→ 服务器实测根因为「飞书表未开应用访问」→ 误用 B014/B015 编号录入（与上批次冲突），已重命名为 B017（P0，绑表静默失败）+ B018（权限自检机制） | Claude |
| 2026-05-23 | 编号修正：把误录的 B014→B017、B015→B018，并修订本表前两行 changelog（之前自相矛盾） | Claude |
| 2026-05-23 | B016（加号无响应）王小熊验收通过 → 关闭，移入「已修复清单」 | Claude |
| 2026-05-25 | 王小熊报"爆款采集失败 ValueError 1.1万"→ 定位 collector.py:209-212 直接 int() → 新增 _parse_count 函数 → 15 个单测全过 → scp + pm2 restart 部署完成 → 录入 B019（已修复待 e2e 验收） | Claude |
| 2026-05-25 | 上条录入时编号冲突：另一批次已在 BUGS.md 加入 B019 ~ B037（飞书表格美化等 19 条），我的爆款采集修复重命名为 **B038**。教训：并发会话下，每次 Edit 前必须重新 grep（即便上一刻 grep 过） | Claude |
| 2026-05-25 | Codex 审查抓到 3 个错误：(1) B038 内行号 209-212 是修复前的旧行号，已改为 94-134 + 252-255  (2) 状态汇总错误地把"已修复待验收"的 bug 计入"待修复"，已按状态字段重新统计  (3) 之前在对话里说"现在 :209-212 是直接 int()"是错的（那是修复前行号）已致歉 | Claude |
| 2026-05-25 | 王小熊问"采集失败保留链接 + PM2 重启原因"。诊断结论：(A) write_failure 功能 app.py:233-243 已实现，服务器实测有效（写入飞书表测试行验证通过），无需修代码  (B) PM2 重启 38 次中 36 次是 graceful（部署/手动），仅 2 次是历史 NameError TOKEN_TO_USER 已在重构中修，当前稳定。两件事均无需开新 bug | Claude |
