# 小红书一键收录 · 运维手册（给王小熊）

> 这份是 admin 视角的运维参考。**团队成员看「使用手册-给团队成员.md」就够**。

---

## 系统概览

```
┌──────────────┐      ┌─────────────────────┐      ┌──────────────┐
│ Chrome 扩展  │ ─→  │ 14.22.112.147:8866  │ ─→  │ 飞书各人表    │
│ （10 个人各装）│      │ uvicorn + PM2        │      │ （每人 1 张） │
└──────────────┘      └─────────────────────┘      └──────────────┘
                              │
                              ├ /opt/xhs-collect/
                              │   ├ app.py (FastAPI)
                              │   ├ lark_writer.py
                              │   ├ collector.py
                              │   ├ admin.py（管理 CLI）
                              │   ├ config.json（含所有用户）
                              │   └ venv/
                              │
                              └ PM2 进程：xhs-collect-api
                                  自启：pm2-root.service (enabled)
```

---

## 加新同事的完整流程

### 第 1 步：让同事建好自己的飞书表

让同事自己在飞书里建一张新的电子表格，命名为「XX 的小红书笔记库」之类，把表 URL 给你。

### 第 2 步：admin CLI 加用户

在你 Mac 上（或 ssh 服务器都行）：

```bash
cd /Users/a86132/Desktop/小红书电商学习文件/小红书工具/xhs-extension-project/server
python3 admin.py add --id zhangsan --name "张三" \
  --sheet-url "https://my.feishu.cn/sheets/XXXXXXXX"
```

输出会包含一串 **Auth Token**，记下来给同事。

### 第 3 步：把 admin.py 改动同步到服务器 + 重启

```bash
scp config.json root@14.22.112.147:/opt/xhs-collect/
ssh root@14.22.112.147 'chmod 600 /opt/xhs-collect/config.json && pm2 restart xhs-collect-api'
```

### 第 4 步：给同事的表预建默认分类

```bash
python3 admin.py init-sheets --id zhangsan
```

这会自动在张三的飞书表里建 7 个默认 sheet（起号/爆款/同行精选/潜力款/标题公式/互动引导/待研究）。

### 第 5 步：发给同事扩展安装包 + Token

把 `xhs-extension-vX.X.X.zip` 和「使用手册-给团队成员.md」+ Token 发给张三。

完事。

---

## 常用 admin 命令

| 命令 | 用途 |
|---|---|
| `python3 admin.py list` | 列出所有用户 |
| `python3 admin.py add --id xxx --name "..." --sheet-url "..."` | 加用户 |
| `python3 admin.py remove --id xxx` | 删用户 |
| `python3 admin.py rotate --id xxx` | 怀疑 token 泄露时，给某用户换新 token |
| `python3 admin.py init-sheets --id xxx` | 给某用户的表建默认分类 sheets |

每次改 `config.json` 后必须重启：

```bash
ssh root@14.22.112.147 'pm2 restart xhs-collect-api'
```

---

## 服务器运维命令

| 操作 | 命令 |
|---|---|
| 看实时日志 | `ssh root@14.22.112.147 'pm2 logs xhs-collect-api --lines 50'` |
| 看进程状态 | `ssh root@14.22.112.147 'pm2 list \| grep xhs'` |
| 重启服务 | `ssh root@14.22.112.147 'pm2 restart xhs-collect-api'` |
| 健康检查 | `curl http://14.22.112.147:8866/api/health` |
| 看磁盘 | `ssh root@14.22.112.147 'df -h /'` |
| 看内存 | `ssh root@14.22.112.147 'free -h'` |

---

## 出问题排查

### 全员都连不上
1. `curl http://14.22.112.147:8866/api/health` 看通不通
2. 不通 → ssh 服务器看 PM2 状态
3. 进程挂了 → `pm2 restart xhs-collect-api`
4. nginx 挂了 → `nginx -s reload`

### 单个用户连不上
1. 看 token 是否在 config.json 里
2. 看用户的飞书表 token 是否正确（在 config.json users 字段）
3. bot「小红书收录助手」是否在该用户飞书表的协作者里
   - 飞书表 → 右上角协作者 → 应能看到"小红书收录助手"
   - 不在 → 让用户手动添加（搜应用名「小红书收录助手」加为可编辑）

### 收录某条笔记失败
1. 打开 popup 顶部「失败 N」点开看错误信息
2. 「未找到 \_\_INITIAL\_STATE\_\_」= 小红书前端改了，找开发者修 collector.py
3. 「URL 中提取不到笔记 ID」= 链接格式不对，让用户重新复制
4. 「网络错误」= 服务器到小红书的网络问题，重试

---

## 安全 / 密钥

| 项 | 位置 | 怎么保护 |
|---|---|---|
| 飞书 App Secret | `config.json` 第 3 行 | 服务器文件 600 权限，别提交 git |
| 用户 Auth Token | `config.json` users 字段 | 同上；怀疑泄露用 `admin.py rotate` |
| 服务器 SSH 密钥 | `~/.ssh/id_rsa` | macOS 自带 keychain 保护 |

定期（每 3-6 个月）建议轮换 App Secret：
1. 去飞书开发者后台重置
2. 更新 `config.json` 的 `app_secret`
3. 重启 PM2
4. 用户不受影响（他们只用 auth_token）

---

## 备份

每周建议手动 dump 一次配置：

```bash
ssh root@14.22.112.147 'cp /opt/xhs-collect/config.json /opt/xhs-collect/config.backup.$(date +%Y%m%d).json'
```

数据本身在飞书云端，不用单独备份。

---

## 升级版本

代码改了之后：

```bash
cd /Users/a86132/Desktop/小红书电商学习文件/小红书工具/xhs-extension-project/server
bash deploy.sh    # 一键部署
```

扩展改了：

```bash
cd /Users/a86132/Desktop/小红书电商学习文件/小红书工具/xhs-extension-project
zip -rq xhs-extension-v2.X.0.zip extension/ \
  -x "*.DS_Store" "**/secrets.js.example"
# 发给团队，让大家移除旧扩展再装新的
```
